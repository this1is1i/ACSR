# 科研推荐系统答辩问答（补充）

> 本文件续接 `QA_2026-05-16_2026-06-02_v1.md`，记录 2026-06-02 起的后续问答。

---

## Q17: 前端状态管理如何实现？登录状态如何持久化？（2026-06-02）（git: bad2152）

### 架构

Pinia（内存响应式）+ localStorage（磁盘持久化），单一 Store：`frontend/src/store/userStore.js`。

### 数据流

```
登录成功 → setAuth(response)
  ├── token    → Pinia token.value  +  localStorage.setItem('token')
  └── userInfo → Pinia userInfo.value + localStorage.setItem('userInfo', JSON)

页面刷新 → useUserStore 初始化
  ├── token    = getStoredToken()     // 从 localStorage 读
  └── userInfo = getStoredUserInfo()  // 从 localStorage 读

每次请求 → request.js 拦截器
  └── config.headers.Authorization = 'Bearer ' + localStorage.getItem('token')

401 响应 → clearStoredAuth() → 清 localStorage + 跳转 /login
```

### 路由守卫

```javascript
// frontend/src/router/index.js:25-39
router.beforeEach((to) => {
  if (!to.meta.public && !token) return '/login'          // 未登录 → 踢登录页
  if (to.meta.roles && 角色不匹配) return '/home'          // 无权 → 踢首页
})
```

### 关键文件

| 文件 | 职责 |
|------|------|
| `frontend/src/store/userStore.js` | Pinia store：token/userInfo 状态 + setAuth/clearToken/fetchProfile |
| `frontend/src/utils/auth.js` | localStorage 读写封装：getStoredToken/setStoredToken/clearStoredAuth |
| `frontend/src/utils/request.js` | Axios 拦截器：自动附加 JWT + 401 处理 |
| `frontend/src/router/index.js` | 路由守卫：public/roles meta 检查 |

---

## Q18: 用户状态为什么是 32+32+32？这些 32 维怎么算出来的？（2026-06-02）（git: bad2152）

### 结构

```
state = concat(interest[:32], history[:32], kg_vector[:32]) = 96 维
base_state_dim = 64 = interest(32) + history(32)
state_dim = 64 + kg_embedding_dim(32) = 96
```

### 兴趣向量（前 32 维）

用户注册时选的关键词 → `hash(关键词)` → 确定性 64 维向量 → 取前 32 维 → 多关键词按 weight 加权平均 → L2 归一化。

```python
# feature_builder.py:192-217
for tag in tags:
    tag_vec = self._hash_tag(tag["interest_tag"], base_state_dim=64)
    vec += tag_vec * tag_weight
vec /= total_weight
# 最终 build_state 时取 vec[:32]
```

### 历史行为向量（中 32 维）

从 behavior_log 查历史 → 每篇论文的预存 32 维 embedding → 按行为类型加权池化（click=0.5, read=1.0, favorite=2.0, 阅读时长每 60s+0.5 上限+2.0）。

```python
# feature_builder.py:219-279
weights = [0.5, 1.0, 2.0][action] + min(duration/60*0.5, 2.0)
history_vec = np.average(vecs, axis=0, weights=weights)
```

### KG 向量（后 32 维）

用户历史论文在 KG 中的 embedding 取平均。

```python
# kg_embedder.py:69-84
user_kg = np.mean([self._embeddings[pid] for pid in history], axis=0)
```

### 为什么是 32

1. **2 的幂**：GPU 矩阵运算内存对齐优化
2. **三段对齐**：兴趣/历史/KG 各 32 维，拼接后各段权重自然均衡
3. **够用但不大**：10 个原始特征投影到 32 维有足够表达能力
4. **历史一致性**：base_state_dim=64, half=32，后续 KG 沿用此数

---

## Q19: 投影矩阵 P 的完整机制（2026-06-02）（git: bad2152）

### P 是什么

P 是一个固定的 10 行 × 32 列矩阵，将 10 维结构特征映射到 32 维嵌入空间。

```python
# kg_embedder.py:46-48 & generate_paper_embeddings.py:173
rng = np.random.default_rng(42)  # 固定种子
P = rng.standard_normal((10, 32)) / sqrt(10)  # Xavier 风格初始化
```

### 为什么除 √10

10 维输入 × 10×32 矩阵 → 输出每个元素是 10 个 N(0,1) 变量的加权和 → 方差膨胀 10 倍 → 除 √10 后方差回到 1。保持输入输出数值尺度一致。

### P 的使用路径

| 路径 | 使用者 | 场景 |
|------|--------|------|
| `checkpoints/projection.npz` | `CandidateGenerator._load_projection_artifacts()` | 推理时元数据回退 |
| `KGEmbedder.__init__()` 自己算 | KGEmbedder | 训练/推理时构建 KG embedding |
| 预存向量直接读 | `CandidateGenerator._load_paper_embedding()` 优先级 1 | 主流路径，跳过 P |

### 完整流水线

```
10 维结构特征 (被引量/关键词数/作者数/年份等，有实际含义)
  → Z-score 标准化 (去量纲，所有维度 N(0,1))
  → 乘以 P (10×32) (线性混合，信息不丢失)
  → L2 归一化 (落到 32 维单位超球面)
  → paper.embedding (32 维向量)
```

### P 不参与训练

P 是固定随机矩阵，不参与梯度下降。原因：
- 随机投影有 Johnson-Lindenstrauss 理论保证：大概率近似保持距离关系
- 10→32 是升维，信息不丢失
- 没有监督信号来训练 P，固定 P 保证稳定性

---

## Q20: Actor-Critic 梯度下降方式（2026-06-02）（git: bad2152）

### 算法：TD(0) Actor-Critic

```python
# agent.py:93-163
# TD 误差
v_s  = critic(s)          # 当前状态估值
td_target = r + γ × critic(s') × (1 - done)   # 实际+预估 = 目标值
δ = td_target - v_s       # TD 误差

# Critic 更新：让估值更准
L_critic = δ²             # 最小化预测误差

# Actor 更新：好的多做，差的少做
L_actor = -log π(a|s) × δ.detach() - β × H(π)
          \_____________/           \______/
           策略梯度（用δ做乘数）      熵正则（防过早收敛）
```

### δ 的含义

| δ | 含义 | 对 Actor 的影响 |
|---|------|----------------|
| δ > 0 | 结果比预期好 | 增大这个动作的概率 |
| δ < 0 | 结果比预期差 | 减小这个动作的概率 |
| δ ≈ 0 | 符合预期 | 微调 |

### 训练循环

每 episode：reset 采样用户 → for step 1..50：Actor 选论文 → 环境模拟反馈 → 算 reward → 算 δ → 更新 Actor + Critic。300 episodes × 50 steps = 最多 15000 次更新。

### 两个优化器

Actor 和 Critic 各有独立的 Adam 优化器（lr=1e-3），各有独立梯度，通过 `δ.detach()` 切断联系——Actor 更新不反传 Critic。

---

## Q21: 数据库设计范式评价（2026-06-03）（git: c513ad2）

### 范式回顾

| 范式 | 核心规则 |
|------|---------|
| 1NF | 字段不可再分 |
| 2NF | 非主键完全依赖主键（消除部分依赖） |
| 3NF | 非主键间不能互相依赖（消除传递依赖） |

### 评价结果

| 范式 | 结果 | 说明 |
|------|------|------|
| 1NF | ⚠️ 部分通过 | paper.keywords/authors/embedding 为 JSON 数组；user.research_interests 为逗号分隔 |
| 2NF | ✅ 通过 | 全部单列主键，无部分依赖 |
| 3NF | ⚠️ 基本通过 | post.like_count/reply_count 冗余计数器；user.research_interests 与 user_interest_history 重复 |

### 主要问题

| 问题 | 文件/表 | 影响 | 建议 |
|------|---------|------|------|
| JSON 数组字段 | `paper.keywords/authors/embedding` | 无法用 SQL 高效查关键词/作者 | 拆 paper_keyword/paper_author 表（当前靠 Neo4j 补位） |
| 逗号分隔字段 | `user.research_interests` | 与 user_interest_history 重复 | 删除该字段，统一从 interest_history 取 |
| ~~冗余计数器~~ | ~~`post.like_count/reply_count`~~ | ~~可能和实际不一致~~ | ✅ 已删除这两个字段，现通过 `PostLikeMapper.batchCountLikes()` / `CommentMapper.batchCountReplies()` 实时统计 |
| ~~缺外键~~ | ~~`rl_training_log.user_id`, `post.reviewed_by`~~ | ~~可能产生孤儿记录~~ | ✅ 已通过 `db_migration.sql` 添加 FK；`rl_training_log.user_id` 改为可空 |

---

## Q22: 推荐排序评估值的计算 + Actor 分数量级问题（2026-06-03）（git: bad2152）

### 原始公式

```
有 KG: final = 0.5 × actor_score + 0.3 × cos_sim + 0.2 × kg_score
无 KG: final = 0.6 × actor_score + 0.4 × cos_sim
```

### 量级差异问题

| 分量 | 来源 | 典型范围 | 均值 |
|------|------|---------|------|
| actor_score | softmax(N≈50) | 0.005 ~ 0.08 | 0.02 |
| cos_sim | L2 归一化后点积 | 0.0 ~ 1.0 | 0.5~0.7 |
| kg_score | KG 向量点积 | 0.0 ~ 1.0 | 0.3~0.5 |

Actor 名义权重 50%，实际贡献约 6%。因为 softmax 把 50 个概率压缩到均值 0.02，而另外两项轻松达到 0.5+。

### 真实影响

候选集被 `CandidateGenerator` 预筛选后，语义相似度挤在窄区间（0.65~0.75）。Actor 虽然绝对值小，但在相邻候选之间提供关键区分度。

---

## Q23: 排序归一化 + 质量门控方案（2026-06-03，✅ 已实现并上线）（git: c513ad2）

### 新排序流程

```
1. 计算三项原始得分
2. 质量门控（OR 逻辑）：cos_sim ≥ min_cos_similarity OR actor ≥ min_actor_score OR kg ≥ min_kg_score
3. 对通过门控的论文，逐项 min-max 归一化到 [0,1]
4. 加权求和 → 降序排列 → Top-K
```

### 改动文件

| 文件 | 操作 | 关键内容 |
|------|------|---------|
| `rl-service/config.py` | +2 配置项 | `min_cos_similarity=0.05`, `min_actor_score=0.001` |
| `rl-service/recommender/ranker.py` | 重写 | `_minmax_normalize()` + 门控→归一化→加权流程 |
| `docs/QA_2026-05-16_2026-06-02_v1.md` | +Q16 | 完整问题诊断、方案设计、边界处理、效果预期 |

### 边界处理

| 场景 | 策略 |
|------|------|
| 门控后 0 篇通过 | 打 warning，回退全量归一化 |
| 某项全等 (max==min) | 赋 0.5 中性分 |
| KG 不可用 | 自动退化为 0.6/0.4 权重 |
| 候选集为空 | 返回空列表 |

### 效果对比（模拟）

```
改造前: A(0.631) > B(0.476) > C(0.032)  Actor贡献48%
改造后: A(1.000) > B(0.731) > C(0.000)  Actor贡献50%（精确等于权重）
```

---

## Q24: Actor 网络在推荐链路中从输入到输出的完整流程（2026-06-03）（git: bad2152）

### 全链路

```
用户 test_user (NLP + GNN)
  │
  ├─ FeatureBuilder.build_state()
  │   ├─ 兴趣 32 维: hash(关键词) → 加权平均
  │   ├─ 历史 32 维: 历史论文 embedding 加权池化
  │   └─ KG 32 维:  历史论文 KG 向量平均
  │   → state = [96维]
  │
  ├─ CandidateGenerator.generate(limit=50)
  │   → 50 篇候选论文 → topic_vector 各取前 32 维 → candidate_features (50, 32)
  │
  └─ Actor.score_candidates(state, candidate_features)
      │
      ├─ state 复制 50 份 → (50, 96)
      ├─ concat(state, paper_features) → (50, 128)
      ├─ 网络: Linear(128→128)×2 → Linear(128→1) → (50,) logits
      └─ softmax → (50,) 概率分布
  
→ ranker: 取 top-k 概率 × 0.5 + cos_sim × 0.3 + kg_score × 0.2
→ 降序排列 → 返回 Top-10
```

### Actor 网络结构

```
actor.py:35-44
  Linear(128→128) → ReLU → LayerNorm → Dropout(0.1)
  Linear(128→128) → ReLU → LayerNorm → Dropout(0.1)
  Linear(128→1)
→ 一个标量 logit
```

---

## Q25: 训练阶段 Actor 和 Critic 为什么能得出不同打分（2026-06-04）（git: bad2152）

### 核心差异

| 维度 | Actor | Critic |
|------|-------|--------|
| 看到论文？ | ✅ 是（concat state + paper→128维） | ❌ 否（只看 state→96维） |
| 输出 | N 个概率（每篇一个） | 1 个标量 V(s) |
| 损失函数 | `-log π × δ - βH` | `δ²` |
| 目标 | 最大化期望奖励 | 准确预测状态价值 |
| 更新信号 | td_error.detach() | 自身预测误差 |
| 推理时 | 决定推荐排序 | 不用 |

### 为什么必须分开

Actor 需要看到论文才能决策（"这篇该不该推"），Critic 评估的是"看到论文之前"的局面价值。如果 Critic 也看到论文特征，V(s) 会混入"已经知道要推什么了"的信息泄露 → td_error ≈ reward → 退化为普通 Policy Gradient → 失去 Actor-Critic 的加速收敛优势。

### 分离评估与决策

演员（Actor）负责演，评论家（Critic）负责评。演员需要知情才能演好，评论家需要置身事外才能客观。两者通过 TD 误差这座桥连接。

---

## Q26: 动作空间设计（2026-06-04）（git: bad2152）

### 定义

动作 = N 篇候选论文中的一个整数索引。`Categorical(probs)` 按概率采样。

```python
action ∈ {0, 1, ..., N-1}   # N = min(action_num, 实际候选数) ≈ 50
```

### 与传统 RL 的区别

| | 传统离散动作空间 | 本系统 |
|---|---------------|--------|
| 大小 | 固定 | 动态（30~50，随候选集变化） |
| 含义 | 固定（动作 3 永远是"开火"） | 变化（动作 3 这轮和下一轮是不同论文） |
| Actor 输出 | 固定 N 个 logit | 动态 N 个 logit |

### 训练 vs 推理

- **训练**：每步按 Categorical 分布随机采样一个动作 → 获取 reward → 更新。50% 概率论文可以探索到。
- **推理**：直接 `torch.topk(probs, k=10)` 取最高 K 个 → 进入 ranker 综合排序。

### Actor 为什么用 pairwise scoring

因为动作空间大小和内容每轮都在变，固定输出头没用。Actor 必须接受论文特征作为输入，动态判断"这篇匹配当前用户吗"。

---

## Q27: 论文拓扑关系如何变成张量（2026-06-04）（git: bad2152）

### 四步变换

```
Neo4j 图结构
  │
  ▼ _extract_structural_features() — 图遍历统计
10 维原始特征
  [cite_in, cite_out, kw_count, kw_reach, author_count,
   author_productivity, venue_popularity, co_author_density,
   topo_depth, recency]
  │
  ▼ Z-score — 全库均值和标准差归一化
  │
  ▼ 投影 P(10×32) — 固定随机矩阵
  │
  ▼ L2 归一化 → 32 维论文张量
paper.embedding
```

### 10 维特征提取（`_extract_structural_features`）

| 维度 | 含义 | 图遍历方式 |
|------|------|-----------|
| [0] cite_in | 被引次数 | 数 rev_adj 中 relation=="cite" 的边 |
| [1] cite_out | 引用次数 | 数 adj 中 relation=="cite" 的边 |
| [2] kw_count | 关键词数量 | 数 adj 中 has_keyword 边 |
| [3] kw_reach | 关键词连通度 | 对每个 has_keyword 边，查 rev_adj[keyword_id] 长度求和 |
| [4] author_count | 作者数量 | 数 rev_adj 中 author_of 边 |
| [5] author_productivity | 作者平均产出 | 对每个作者查 adj[author_id] 边数 / author_count |
| [6] venue_popularity | 场所影响力 | 对 publish_in 边，查 rev_adj[venue_id] 长度 |
| [7] co_author_density | 合著密度 | 作者间 co_author 边数 / 最大可能对数 |
| [8] topo_depth | 引用链深度 | BFS 沿 cite 出边，最深 5 层 |
| [9] recency | 新旧度 | (year-2010)/15 |

### 无 KG 时的回退

```python
# kg_embedder.py:199-242
extract_features_from_metadata()
→ citation_count / max_cite  # 仅有的可用维度
→ 其他 KG 相关维度填 0.0
```

---

## Q28: 学习路径计算 + 掌握度衰减 + 3D 力导向图（2026-06-04）（git: bad2152）

### 后端 → Python 数据流

```
VisualizationServiceImpl.buildKnowledgeGraph()
  ├─ 查用户历史论文的 AMiner ID
  ├─ POST /learning-path → Python
  │   ├─ PathBuilder.build_path()
  │   │   ├─ _extract_known_keywords(): 从历史论文提取关键词+频率
  │   │   ├─ get_keyword_cluster(): 查目标关键词的共现词+论文
  │   │   ├─ _build_path_nodes(): 构建 4 层路径节点
  │   │   │   depth=0: 已掌握关键词 (mastery=频率, 取前5)
  │   │   │   depth=1: 相关关键词 (未知的 mastery=0)
  │   │   │   depth=2: 目标关键词
  │   │   │   depth=3: 目标论文
  │   │   ├─ _build_path_edges(): 只连相邻深度层
  │   │   └─ coverage = 已知关键词∩相关关键词 / 相关关键词总数
  │   │
  │   └─ KnowledgePropagation
  │       ├─ batch_update(history): 按时间顺序批量传播
  │       │   time_weight = (i+1)/n  (越早读权重越低——遗忘)
  │       │   event_multiplier: read=1.0, favorite=1.5, write=2.0
  │       │
  │       ├─ update_mastery(paper_id, delta): 直接更新论文节点
  │       │
  │       └─ _propagate(node_id, delta, hop): 递归沿边扩散
  │           max_hops=3, decay=0.6 per hop
  │           边类型系数: has_keyword=0.8, cite=0.4, publish_in=0.3
  │           new_delta = delta × decay × rel_coeff × edge_weight
  │           delta < 1e-4 停止
  │
  └─ 返回 JSON: {nodes, edges, learningPath, pathNodes, pathEdges}
```

### 掌握度传播公式

```
mastery(neighbor) += delta_source × 0.6^hop × edge_type_coeff × edge_weight
```

### 颜色映射

```
mastery=0.0 → #3B82F6 (蓝)   未学习
mastery=0.5 → #F59E0B (橙)   学习中
mastery=1.0 → #10B981 (绿)   已掌握
```

### 3D 力导向图构建（`KnowledgeGraph.vue:153-293`）

| 步骤 | 操作 |
|------|------|
| 数据归一化 | 优先 pathNodes/pathEdges；边格式 src/dst → source/target |
| 分层布局 | `dagMode('td')` 自上而下，dagLevelDistance=50 |
| 节点渲染 | 关键词=球体（大小 4~5），论文=盒子（4×4×4）；color + emissiveIntensity 来自服务端 |
| 标签 | CanvasTexture sprite 浮在节点上方 |
| 路径播放 | setInterval 每步高亮 visited/active 节点+边，镜头自动追踪 |
| 交互 | 点击节点→镜头拉近+详情面板；点击背景→取消选中 |

---

## 后续问题索引

| Q# | 主题 | 日期 |
|----|------|------|
| Q17 | 前端状态管理与登录持久化（Pinia + localStorage） | 2026-06-02 |
| Q18 | 用户特征 32+32+32 维度的计算 | 2026-06-02 |
| Q19 | 投影矩阵 P 的完整机制 | 2026-06-02 |
| Q20 | Actor-Critic 梯度下降方式 | 2026-06-02 |
| Q21 | 数据库设计范式评价 | 2026-06-03 |
| Q22 | 推荐排序评估值 + Actor 分数量级问题 | 2026-06-03 |
| Q23 | 排序归一化 + 质量门控方案（已实现） | 2026-06-03 |
| Q24 | Actor 网络从输入到输出的完整推荐链路 | 2026-06-03 |
| Q25 | 训练阶段 A 网络与 C 网络差异原因 | 2026-06-04 |
| Q26 | 动作空间设计 | 2026-06-04 |
| Q27 | 论文拓扑关系 → 张量结构 | 2026-06-04 |
| Q28 | 学习路径计算 + 掌握度衰减 + 3D 力导向图 | 2026-06-04 |
| Q29 | 三端模块架构与功能调用链路 | 2026-06-04 |

---

## Q29: 三端模块架构与功能调用链路（2026-06-04）（git: bad2152）

详见独立文档：[`docs/architecture.md`](architecture.md)，包含：

- **第一部分**：三端模块全景（前端 10 Views + 15 Components + 9 API + Store/Router/Utils；后端 11 Controllers + 7 Services + 10 Mappers + 6 DTOs + 7 Configs；Python 26 文件 40 类 20 函数）
- **第二部分**：10 大功能的 Mermaid 时序图 —— 个性化推荐、学习路径+3D 可视化、用户认证、行为追踪、社区论坛、实时私信、作者认领、论文搜索、RL 训练、管理员后台
- **第三部分**：三端跨功能依赖图（Mermaid graph TB）
- **第四部分**：调用关系速查表（View→API、Service→Mapper、Python 入口→依赖链）

---

## Q30: 线程池异步处理与推荐服务可靠性保障（2026-06-05）（git: bad2152）

### 线程池使用位置

项目中有 **3 处** 使用线程池/异步机制：

#### 1. Spring Boot 全局异步开关

```java
// ResearchApplication.java:10
@EnableAsync
public class ResearchApplication { ... }
```

声明式开启，让 `@Async` 注解生效。

#### 2. 专用线程池 Bean（`AppConfig.java:26-36`）

```java
@Bean("taskExecutor")
public ThreadPoolTaskExecutor taskExecutor() {
    executor.setCorePoolSize(4);           // 常驻 4 线程
    executor.setMaxPoolSize(8);            // 峰值 8 线程
    executor.setQueueCapacity(50);         // 等待队列 50
    executor.setThreadNamePrefix("async-rec-");
    executor.setRejectedExecutionHandler(
        new ThreadPoolExecutor.CallerRunsPolicy()  // 核心：队列满时调用线程自己跑
    );
}
```

线程池已就绪但当前 **无 `@Async` 的直接使用者**——这是一个预留能力，需要时在 Service 方法加 `@Async("taskExecutor")` 即可切入。

#### 3. Python 训练异步（`server.py:199-236`）

```python
@router.post("/train")
async def trigger_train(request, background_tasks: BackgroundTasks):
    def _run_train():
        train(config=config_copy, stop_controller=controller)

    background_tasks.add_task(_run_train)   # FastAPI 后台任务队列
    return TrainResponse(message="训练已在后台启动")
```

FastAPI `BackgroundTasks` 将训练丢到后台线程，API 立即返回。`_training_status` 全局变量作跨线程状态共享，`GET /model/info` 可实时查询进度。

### AppConfig 四层可靠性保障

#### 第一层：连接超时（5s）

```java
.setConnectTimeout(Timeout.ofMilliseconds(connectTimeout))  // 默认 5000ms
```

Python 进程挂了时不等操作系统默认 30s，5 秒就放弃——快速识别不可达。

#### 第二层：读取超时（10s）

```java
.setResponseTimeout(Timeout.ofMilliseconds(readTimeout))  // 默认 10000ms
```

Python 接收了请求但内部卡死时，10 秒没数据就断开。两超时合力：无论 Python 处于什么异常状态，调用方最多 ~15s 一定能拿到结果或异常。

#### 第三层：Apache HttpClient 连接池

```java
HttpClients.custom().setDefaultRequestConfig(requestConfig).build()
```

用 Apache HttpClient（带连接池）而非 JDK `HttpURLConnection`，TCP 连接可复用，避免高频调用时的端口耗尽和握手开销。

#### 第四层：CallerRunsPolicy

```java
executor.setRejectedExecutionHandler(new ThreadPoolExecutor.CallerRunsPolicy())
```

四种拒绝策略中唯一"不丢请求"的选择：

| 策略 | 队列满时行为 | 推荐场景 |
|------|------------|---------|
| `AbortPolicy` | 抛异常 | ❌ |
| `DiscardPolicy` | 静默丢弃 | ❌ |
| `DiscardOldestPolicy` | 丢最早 | ❌ |
| **`CallerRunsPolicy`** | 调用线程自己跑 | ✅ 慢但不丢 |

推荐请求是用户等待的结果——宁可慢不能让用户看空白页。

### PythonRecClient 的 try/catch 兜底

```java
public RecResponse getRecommendations(...) {
    try {
        return restTemplate.postForEntity(...).getBody();
    } catch (ResourceAccessException e) {  // 连接超时/被拒 → 命中第一层
        return null;
    } catch (Exception e) {               // 读超时/序列化异常 → 命中第二层
        return null;
    }
}
```

四个方法（`getRecommendations` / `triggerTraining` / `getLearningPath` / `getModelInfo`）**全部** try/catch → null/false，不向上抛异常。

### RecommendServiceImpl 的降级

```java
if (pyResp != null && pyResp.getRecommendations() != null) {
    items = assembleFromPython(pyResp.getRecommendations());     // 正常
} else {
    items = fallbackPopularRecommendations(k);                   // 降级
    // 返回本地热门论文，score=0.5，reason="推荐服务暂时不可用"
}
```

### 可靠性全景

```
用户请求
  → Tomcat 线程 → RecommendServiceImpl
      → PythonRecClient (RestTemplate: 连接池+5s/10s超时)
          ├── 成功 → 正常返回
          └── 异常 → return null
      → pyResp==null → fallbackPopularRecommendations()
          → 本地 paper 表热门论文 → "热门推荐（服务维护中）"
```

**五层防御：超时 + 连接池 + CallerRunsPolicy + try/catch null + 本地降级。即使 Python 完全宕机，用户仍看到推荐列表而非 500 错误。**

### 后续问答索引

| Q# | 主题 | 日期 |
|----|------|------|
| Q30 | 线程池异步处理与推荐服务可靠性保障 | 2026-06-05 |
| Q31 | 数据库三问：特征快照来源、兴趣权重计算、训练日志不再更新 | 2026-06-05 |
| Q32 | 数据库整改实施方案（13 文件改动） | 2026-06-05 |
| Q33 | 用户兴趣向量为何算 64 维再截 32 维 | 2026-06-05 |
| Q34 | 线程池异步处理与推荐服务可靠性保障 | 2026-06-05 |
| Q35 | 三端完整代码函数级文档 | 2026-06-05 |
| Q36 | 行为向量与KG向量的区别 + 兴趣向量hash机制分析 | 2026-06-05 |

---

## Q31: 数据库三问（2026-06-05）（git: c513ad2）

### 问题 1：user_feature_snapshot 每条数据怎么来的？

**唯一写入路径**：`MySQLDataSource.cache_feature()` → `FeatureBuilder._build_features_from_mysql()`。

触发时机：每次推荐请求或训练 episode reset 时，`FeatureBuilder` 尝试从 MySQL 实时构建用户向量，构建完成后顺手写入缓存。

完整链路：

```
FeatureBuilder.get_user_features(user_id)
  → _build_features_from_mysql()
      │
      ├── 1. 尝试读缓存 (6h TTL)
      │      get_cached_feature("interest") + get_cached_feature("history")
      │      命中 → 直接返回，不写入
      │
      ├── 2. 缓存未命中 → 实时构建
      │      _build_interest_from_mysql() → 64-dim interest_vec
      │      _build_history_from_mysql()  → 64-dim history_vec
      │
      └── 3. 写入缓存
             cache_feature(user_id, "interest", interest_vec, "computed")
             cache_feature(user_id, "history",  history_vec,  "computed")
```

`cache_feature()` 使用 `INSERT ... ON DUPLICATE KEY UPDATE`（`mysql_data.py:118-127`），靠 `uk_user_type`（user_id + feature_type 唯一键）保证每个用户每种类型只有一条记录，重复构建时更新 `feature_vector` 和时间戳。

TTL 逻辑在 `get_cached_feature()`（`mysql_data.py:93-110`）：

```sql
WHERE user_id = %s AND feature_type = %s
  AND updated_at >= NOW() - INTERVAL 6 HOUR
```

超过 6 小时的缓存视为过期，重新构建。

**缓存来源标记**：所有自动构建的特征 source 标记为 `"computed"`。seed 数据中已有 4 个用户的预填记录（user 1/2/6/7/8/9/3/10/5 共 9 个用户各有 interest+history 两条，即表中有 18 条记录，编码到 snapshot id 1~46）。

### 问题 2：user_interest_history 的 weight 字段

**设计意图**：weight 代表用户对该兴趣标签的"关注程度"，理论上应该根据用户在该领域的行为密度动态调整。这为未来的**兴趣衰减和权重更新**留下了接口。

**当前实现**：整个项目中 `new UserInterestHistory()` 只出现了一次——在 `UserServiceImpl.register()` 第 68 行，**硬编码 weight=0.5**：

```java
// UserServiceImpl.java:65-71
UserInterestHistory history = new UserInterestHistory();
history.setUserId(user.getId());
history.setInterestTag(trimmed);
history.setWeight(0.5);       // ← 固定 0.5，从未被动态更新
history.setSource("register");
history.setRecordDate(today);
userInterestHistoryMapper.insert(history);
```

**现状**：weight 从未被任何行为追踪逻辑更新——用户无论在该领域点击了多少论文、收藏了多少，weight 始终是注册时的 0.5。这是一个**设计了但未激活**的字段。`feature_builder.py` 读 weight 做加权平均，但由于所有标签权重一样，实际等价于不加权平均。

**如何用 user_interest_history 替代 user.research_interests**：

当前替代查询已存在于 `MySQLDataSource.get_user_interest_tags()`（`mysql_data.py:63-73`）：

```sql
SELECT interest_tag, weight, source, record_date
FROM user_interest_history
WHERE user_id = ?       -- JOIN user_contacts 拿到 contact_id
ORDER BY record_date DESC, weight DESC
```

如果要替换合作者匹配中直接解析 `user.research_interests`（逗号分隔字符串）的逻辑，分三步：

**Step 1 — 替代"获取当前用户研究方向"查询**（替换 `user.research_interests` 字段读取）：

```sql
-- 原逻辑: user.research_interests → split(",")
-- 新逻辑: 从 user_interest_history 取 source='register' 的记录
SELECT interest_tag, MAX(weight) as weight
FROM user_interest_history
WHERE user_id = ? AND source = 'register'
GROUP BY interest_tag
ORDER BY weight DESC
```

**Step 2 — 替代"合作者匹配 overlap 计算"**（替换 `PrivateMessageServiceImpl.getRecommendedCollaborators()` 中对 `research_interests` 的 split 操作）：

```sql
-- 批量查询同角色用户的兴趣标签，计算与当前用户的重叠度
SELECT u.id as user_id,
       COUNT(DISTINCT h1.interest_tag) as overlap_count
FROM user u
JOIN user_interest_history h1 ON u.id = h1.user_id
WHERE u.role = ?                          -- 同角色
  AND u.id != ?                           -- 排除自己
  AND u.id NOT IN (联系人列表)              -- 排除已有联系人
  AND h1.interest_tag IN (                 -- 与当前用户共享的兴趣
    SELECT interest_tag FROM user_interest_history WHERE user_id = ?
  )
GROUP BY u.id
ORDER BY overlap_count DESC
LIMIT 2
```

**Step 3 — 删除 `user.research_interests` 字段**（可选，评估无其他依赖后执行）。

### 问题 3：rl_training_log 为什么不再更新了？

**直接原因：Python 训练脚本从未写入这张表。**

搜索结果确认：`rl-service/` 下整个 Python 代码库中对 `rl_training_log` 的引用次数为 **零**。

Python 训练日志写到了哪里？

```python
# train.py:249 → logger.py:21-38
train_logger = TrainingLogger(log_dir="logs/", experiment_name="ac_recommender")
# 输出:
#   控制台 stdout
#   logs/ac_recommender.log           ← 文本日志
#   logs/ac_recommender_history.json  ← JSON 历史指标
```

`rl_training_log` 表里原有的 5 条记录（episode 100/200/300/400/500）来自早期 seed 数据的静态 INSERT。

**2026-06-07 更新**：选项 B 已实施。`train.py` 训练结束时调用 `MySQLDataSource.insert_training_log()` 写入一条摘要记录（含 episode、reward、loss、model_version），`user_id` 为 NULL（系统级训练）。前端 AdminConsole "模型训练" tab 可触发训练并轮询 `GET /model/info` 获取结果。`db_migration.sql` 已将 `rl_training_log.user_id` 改为可空，允许系统训练记录。

### 后续问答索引

---

## Q32: 数据库整改实施方案（2026-06-05）（git: c513ad2）

### SQL 迁移脚本
文件: `backend/src/main/resources/db_migration.sql`，用户自行在 MySQL 执行：

```sql
ALTER TABLE rl_training_log ADD CONSTRAINT fk_train_user
    FOREIGN KEY (user_id) REFERENCES user(id) ON DELETE RESTRICT ON UPDATE RESTRICT;
ALTER TABLE post ADD CONSTRAINT fk_reviewed_by
    FOREIGN KEY (reviewed_by) REFERENCES user(id) ON DELETE SET NULL ON UPDATE RESTRICT;
ALTER TABLE post DROP COLUMN like_count;
ALTER TABLE post DROP COLUMN reply_count;
```

### 任务 1: user_interest_history 替代 user.research_interests（6 文件）

| 文件 | 改动 |
|------|------|
| `UserInterestHistoryMapper.java` | +`findTagsByUserId()` 查单用户标签; +`findTagsByUserIds()` 批量查 |
| `UserServiceImpl.java` | register 删除 `setResearchInterests`; updateProfile 改为写 interest_history(source='profile_update'); getProfile/searchUsers 从 interestHistory 拼字符串 |
| `UserMapper.java` | searchUsers SQL: `research_interests LIKE` → `LEFT JOIN user_interest_history ... uih.interest_tag LIKE` |
| `PrivateMessageServiceImpl.java` | 注入 UserInterestHistoryMapper; parseInterests 改接 userId |
| `AdminServiceImpl.java` | 注入 UserInterestHistoryMapper; listUsers 批量查标签; toAdminUser 增加 interestMap |

### 任务 2: 删除 post.like_count/reply_count（5 文件）

| 文件 | 改动 |
|------|------|
| `Post.java` | 删除 likeCount/replyCount 字段 |
| `PostLikeMapper.java` | +`batchCountLikes(postIds)` |
| `CommentMapper.java` | +`batchCountReplies(postIds)` |
| `CommunityServiceImpl.java` | 删除所有 setLikeCount/setReplyCount/计数器增减; listPosts/searchPosts/listMyPosts 加批量计数排序; toggleLike 简化为纯 INSERT/DELETE; 新增 toCountMap(); toPostItem/resolveComparator 增加 count maps |
| `AdminServiceImpl.java` | 注入 PostLikeMapper+CommentMapper; listPosts 加批量计数; toPostItem 增加 count maps; 新增 toCountMap() |

### 任务 4: 管理员训练按钮 + rl_training_log 激活（✅ 已实施）

| 文件 | 改动 |
|------|------|
| `frontend/src/api/recommend.js` | +`triggerTraining(episodes)`, +`getModelInfo()` |
| `frontend/src/views/AdminConsole.vue` | +"模型训练" tab: el-input-number + 按钮 → 2s轮询 → el-dialog展示训练结果 |
| `rl-service/data/mysql_data.py` | +`insert_training_log(episode, reward, loss, model_version, user_id=None)` |
| `rl-service/train.py` | train() 改为返回 `(agent, metrics)`; 训练结束写一条 rl_training_log; metrics 含 total_episodes/best_reward/final_avg_loss/model_version |
| `rl-service/api/server.py` | _run_train 解包 metrics; 更新 `_training_status["best_reward"]`（修复从未更新的 bug） |
| `backend/.../db_migration.sql` | `rl_training_log.user_id` 改为可空（系统训练不关联用户），FK 改为 `ON DELETE SET NULL` |

---

## Q33: 用户兴趣向量为何算 64 维再截 32 维（2026-06-05）（git: bad2152）

### 原因：64 维是"全特征空间"，32 维仅是状态内部的分割

`FeatureBuilder` 内部一切工作在 `base_state_dim=64` 维度：`_hash_tag(tag, 64)` → 64 维单位向量；`_paper_attr_vec(paper)` → 硬编码 64 维词袋向量；`_build_interest_from_mysql()` → 64 维兴趣向量；`_build_history_from_mysql()` → 64 维历史向量。

但同一份 64 维向量有两个消费者：

| 消费者 | 使用的维度 | 位置 |
|--------|-----------|------|
| **Actor 状态拼接** | 前 32 维 | `build_state()`: `interest[:32] + history[:32]` |
| **候选检索余弦相似度** | 完整 64 维 | `CandidateGenerator.generate(user_embedding=interest_vector)`（完整 64 维点积检索） |
| **排序余弦相似度** | 完整 64 维 | `ranker.rank()`: `base_state[:64] @ topic_matrix` |

如果直接算 32 维，候选检索和排序的余弦相似度只能在 32 维空间做——hash 桶减半，每个桶的碰撞概率翻倍，区分度下降。算 64 取 32 让检索用高分辨率空间，状态只取前一半。`build_item_vector()` 也返回完整 64 维（`_pad_to_base_dim` 将 32 维论文 embedding 后补零到 64），确保 topic_vector 与 interest_vector 维度对齐。

---

## Q34: 线程池异步处理与推荐服务可靠性保障（2026-06-05）（git: bad2152）

### 线程池使用位置（3 处）

1. **Spring `@EnableAsync`**（`ResearchApplication.java:10`）：声明式全局异步开关
2. **专用线程池 `taskExecutor`**（`AppConfig.java:26-36`）：core=4, max=8, queue=50, CallerRunsPolicy（队列满时调用线程自己跑，不丢请求），线程前缀 "async-rec-"。当前无 `@Async` 直接使用者，属于预留能力
3. **Python FastAPI `BackgroundTasks`**（`server.py:232`）：`background_tasks.add_task(_run_train)` 将训练丢到后台线程，API 立即返回"训练已启动"

### AppConfig 四层可靠性保障

| 层 | 配置 | 作用 |
|----|------|------|
| 连接超时 | `connectTimeout=5000ms` | Python 进程挂了不等 OS 默认 30s，5s 放弃 |
| 读取超时 | `readTimeout=10000ms` | Python 内部卡死最多等 10s |
| 连接池 | Apache HttpClient（`HttpClients`） | TCP 连接复用，避免高频调用端口耗尽和握手开销 |
| CallerRunsPolicy | 线程池拒绝策略 | 队列满时由 Tomcat 线程同步执行——慢但不丢 |

### 调用方兜底

- **PythonRecClient**：4 个方法全部 try/catch → null/false，不抛异常
- **RecommendServiceImpl**：`pyResp == null` → `fallbackPopularRecommendations(k)` → 本地热门论文（score=0.5, reason="推荐服务暂时不可用"）

> **五层防御：超时 + 连接池 + CallerRunsPolicy + try/catch null + 本地降级。即使 Python 完全宕机，用户仍看到推荐列表而非 500 错误。**

---

## Q35: 三端完整代码函数级文档（2026-06-05）（git: bad2152）

详见独立文档：[`docs/function-reference.md`](function-reference.md)，包含：

- **前端 JS**：9 个 API 模块(29函数) + Store(9) + Router(12路由+守卫) + 4个Utils 的全部函数签名和 HTTP 端点
- **前端 Vue 布局**：10个View → 组件 → API 调用关系速查表
- **后端 Java**：11 Controller(45端点) + 7 Service(40+方法) + 12 Mapper(20+SQL) + PythonRecClient(4跨服务调用) + 7 Config + 9 Entity + 6 DTO 的完整方法文档
- **Python 服务**：26个 .py 文件、40+类、所有公开方法，关键函数逐行讲解（Actor.score_candidates, Agent.update, RLRanker.rank 五步归一化流程, FeatureBuilder 行为加权池化, KGEmbedder 10维图特征提取, KnowledgePropagation 递归传播）

---

## Q36: 行为向量与KG向量的区别 + 兴趣向量hash机制分析（2026-06-05）（git: bad2152）

### 行为向量 vs KG 向量

| | 兴趣向量 | 历史行为向量 | KG 向量 |
|---|---|---|---|
| **来源** | `user_interest_history` 标签 | `behavior_log` 中论文的 embedding | Neo4j 图结构的 KG embedding |
| **构建** | 标签→hash→单位向量→加权平均 | 论文32维预存向量→行为加权池化 | 论文10维图特征→Z-score→P(10×32)→均值池化 |
| **权重** | weight(注册写0.5, seed有0.5~5.0) | click=0.5, read=1.0, favorite=2.0, 每60s+0.5(上限+2.0) | 无权重，直接均值 |
| **含义** | "用户声称对什么感兴趣" | "用户实际读了什么论文" | "用户读的论文在图谱中处于什么位置" |
| **更新** | 注册/编辑资料时一次性 | 每次行为实时 | 随历史列表实时重算 |

### 兴趣向量 hash 机制

`_hash_tag(tag, dim)` 将标签字符串 → `hash()` → 32-bit int 种子 → PRNG → dim 个 N(0,1) 数 → L2 归一化。**确定性但无语义**：同一标签每次相同向量，不同标签不同向量。共享标签的用户向量有共同分量（余弦相似度>0），无交集标签的用户接近正交。这种技术叫**随机索引（Random Indexing）**。

**局限性**：标签间无语义距离（"Deep Learning"和"Reinforcement Learning"与"Deep Learning"和"Quantum Physics"的关系相同）。本项目通过注册时强制从 Neo4j 46 个 Keyword 节点选择来规避"NLP"与"Natural Language Processing"不匹配问题。改进方向（暂不实施）：从 KG 关键词共现矩阵学语义 embedding，或通过关联高被引论文用 `paper.embedding` 均值替代 hash。

---

### 后续问答索引

| Q# | 主题 | 日期 |
|----|------|------|
| Q31 | 数据库三问 | 2026-06-05 |
| Q32 | 数据库整改实施方案 | 2026-06-05 |
| Q33 | 兴趣向量为何算 64 维再截 32 维 | 2026-06-05 |
| Q34 | 线程池异步与推荐服务可靠性 | 2026-06-05 |
| Q35 | 三端函数级文档 | 2026-06-05 |
| Q36 | 行为向量vs KG向量 + hash分析 | 2026-06-05 |
| Q37 | 密码加密机制与测试账号密码 | 2026-06-07 |

---

## Q37: 密码加密机制与测试账号密码（2026-06-07）（git: c513ad2）

### 加密机制

使用 Spring Security `BCryptPasswordEncoder`（strength=10，哈希前缀 `$2a$10$`）。注册时 `passwordEncoder.encode()` 加密存储，登录/改密时 `passwordEncoder.matches()` 校验。关键代码：

```java
// SecurityConfig.java:97-98
@Bean
public PasswordEncoder passwordEncoder() {
    return new BCryptPasswordEncoder();
}

// UserServiceImpl.java:47 — 注册
user.setPassword(passwordEncoder.encode(request.getPassword()));

// UserServiceImpl.java:77 — 登录校验
passwordEncoder.matches(request.getPassword(), user.getPassword())

// UserServiceImpl.java:226-229 — 改密
if (!passwordEncoder.matches(req.getOldPassword(), user.getPassword()))
    throw ...;
user.setPassword(passwordEncoder.encode(req.getNewPassword()));
```

新增 `PUT /api/user/password` 端点（`UserController.java`），接受 `{oldPassword, newPassword}`，需要旧密码验证。

### 测试账号密码

| 用户范围 | 明文密码 | BCrypt Hash（前缀） |
|---------|---------|-------------------|
| id 1–10 (admin, test_user, xixihaha, researcher_li, student_wang, student_zhang, researcher_chen, alice_prof, new_user_test) | `admin123` | `$2a$10$lqOCFA3Qd6/...` |
| id 11–16 (Mnih,V, Vaswani,A, Devlin,J, Schulman,J, Shazeer,N, Badia,A — 来自 `seed_claim_test_data.sql`) | `123456` | `$2b$12$QjZY15zfJqurPw...` |
| id 17 (student_zhao，通过注册 API 创建) | `qwer1234` | `$2a$10$rvc9LkCCDmg...` |

### 历史问题

id 1–10 全部使用**完全相同的 BCrypt 哈希值**——SQL 脚本中将同一个预计算哈希复制粘贴的结果，而非逐个调用 `encode()` 生成。这导致所有老用户共享同一密码 `admin123`。实际注册的新用户（如 student_zhao）每次产生独立的 salt 和哈希。

### 改密端点

```
PUT /api/user/password  (Auth required)
Body: { "oldPassword": "...", "newPassword": "..." }
Response: { "code": 0, "message": "密码修改成功", "data": null }
```

前端 EditProfile.vue 新增"修改密码"卡片，含原密码/新密码/确认新密码三个字段，修改成功后清除登录态并跳转登录页。

---

## Q38: AC 网络的实质是什么？训练中学习的是什么？（2026-06-07）（git: 5ab8357）

### 物理实质：两个 MLP，约 6.6 万参数

AC 网络不是一个"网络"，而是**两个独立的三层 MLP**，各自用 Adam 优化器独立更新：

| 组件 | 文件 | 结构 | 参数量 |
|------|------|------|--------|
| Actor | `models/actor.py` | `Linear(128→128)→ReLU→LayerNorm→Linear(128→128)→ReLU→LayerNorm→Linear(128→1)` | ~33K |
| Critic | `models/critic.py` | `Linear(96→128)→ReLU→LayerNorm→Linear(128→128)→ReLU→LayerNorm→Linear(128→1)` | ~33K |

两者加起来约 6.6 万参数——这是一个非常小的网络（对比：ResNet-18 有 1100 万参数）。之所以够用，是因为输入不是原始像素而是已经高度结构化的 96/128 维特征向量。

### 训练中变化的只有一个东西：权重矩阵

每轮训练中，以下参数被 Adam 优化器更新：

- **Actor 的 6 个权重/偏置矩阵**（3 个 Linear 层 × 2）：`actor.py:36-45`
- **Critic 的 6 个权重/偏置矩阵**（3 个 Linear 层 × 2）：`critic.py:31-40`

除此之外**没有任何东西变化**——没有新神经元、没有结构变化、没有记忆存储。所有的"学习"都编码在这约 6.6 万个浮点数中。

### 学习的是什么：一个打分函数

**Actor 学习的是**：给定用户状态 s 和论文特征 p，输出这篇论文被用户喜欢的概率。

```
π(a_i|s) = softmax( Actor([s | p_i]) )  对 i=1..N 篇候选论文
```

训练前，Actor 的权重是随机正交初始化的（`actor.py:52`，gain=0.01），所有论文的概率几乎均匀。训练后，Actor 学会了区分：与用户兴趣向量余弦相似度高的论文、KG 拓扑距离近的论文会获得更高的 softmax 概率。

**Critic 学习的是**：给定用户状态 s，估计从这个状态开始能获得的期望累积奖励。

```
V(s) = Critic(s)  → 标量
```

### 学习机制：TD 误差是 Actor 和 Critic 之间的唯一桥梁

核心代码在 `agent.py:93-163`，单步更新流程：

```
1. Actor 选论文 a_t → 环境给出奖励 r_t
2. Critic 估计 V(s_t) 和 V(s_{t+1})
3. 计算 TD 误差: δ = r_t + γ·V(s_{t+1}) - V(s_t)
   - δ > 0: 实际比预期好 → 这个动作值得鼓励
   - δ < 0: 实际比预期差 → 这个动作应该抑制
4. Critic 更新: 最小化 δ²（让 V(s) 预测更准）
5. Actor 更新: 最大化 log π(a_t|s_t) · δ（好动作概率↑，坏动作↓）
   + 熵正则项 -β·H(π) 防止过早收敛到单一推荐
```

**关键设计**：Actor 更新用的是 `δ.detach()`（`agent.py:149`），即 δ 的梯度不回流到 Critic。这意味着 Actor 和 Critic 的权重更新是解耦的——Critic 只管把 V(s) 估准，Actor 只管利用 δ 的符号和大小来调整策略。

### 驱动学习的是什么：六维奖励信号

Actor 和 Critic 本身没有"对错"概念——它们只会朝着更高奖励的方向优化。奖励信号来自 `utils/reward.py` 的加权线性函数：

```
r = α·click + β·favorite + γ·read_time + δ·topic_match + η·long_term_value + ζ·kg_topology
  = 1.0·click + 2.0·favorite + 0.5·read_time + 3.0·topic_match + 1.5·long_term + 2.0·kg_topology
```

训练时这些信号并非来自真实用户（`rec_env.py:259-274`），而是用 `cos_sim(user_interest, paper_topic)` 模拟的——兴趣越匹配，模拟点击/收藏概率越高。

### 训练收敛后保存的是什么

`agent.py:198-212` 的 `save_model()` 保存到 `checkpoints/ac_model.pth`，内容包括：

```python
{
    "actor_state_dict":  {...},   # Actor 所有权重
    "critic_state_dict": {...},   # Critic 所有权重
    "train_step": 15000,          # 总训练步数
    "episode_count": 300,         # 总 episode 数
    "config": {...},              # 超参数快照
}
```

推理时 `load_model()` 恢复这两个 state_dict，Actor 的 `score_candidates()` 即可直接用于排序——Critic 在推理阶段不参与计算。

### 一句话总结

AC 网络的实质是**两个小型 MLP 的权重矩阵**。训练改变的唯一东西就是这 ~66K 个浮点数。Actor 学会了一个从 (用户状态, 论文特征) → 推荐概率的映射，Critic 学会了一个从用户状态 → 期望累积奖励的映射。两者通过 TD 误差 δ 耦合：δ 是 Critic 的预测目标，也是 Actor 更新幅度的缩放因子。

---

## Q39: 学习路径的四层节点如何生成？掌握度传播算法如何计算？（2026-06-07）（git: 5ab8357）

### 相关文件

| 文件 | 职责 |
|------|------|
| `learning_path/path_builder.py` | 四层节点生成、边构建、序列化 |
| `learning_path/propagation.py` | 掌握度传播算法、颜色映射、发光强度 |
| `knowledge_graph/graph_query.py:123-172` | `get_keyword_cluster()` — 关键词聚类查询 |

### 四层节点的生成算法

入口：`PathBuilder.build_path()`（`path_builder.py:62-125`），流程如下：

```
1. _extract_known_keywords(history)
   → 从用户历史论文中提取关键词 + 出现频率作为初始掌握度

2. query.get_keyword_cluster(target_topic)
   → 在 KG 中查找目标关键词的共现关键词和相关论文

3. _build_path_nodes(known, target, related, papers)
   → 按深度分四层构建节点序列

4. _build_path_edges(nodes)
   → depth → depth+1 的相邻层之间建边
```

四层节点的生成逻辑在 `_build_path_nodes()`（`path_builder.py:152-208`）：

| 层 | depth | 节点来源 | 数量 | 初始掌握度 |
|----|-------|---------|------|-----------|
| **Layer 0 — 已掌握** | 0 | 用户历史论文中的关键词（最多 5 个） | ≤5 | `_extract_known_keywords()` 计算 |
| **Layer 1 — 相关知识** | 1 | 与目标关键词共现的关联关键词（`get_keyword_cluster` 返回） | ≤k (默认5) | 若在历史中出现过则取其频率，否则 0.0 |
| **Layer 2 — 目标主题** | 2 | 目标关键词本身 | 1 | 若在历史中出现过则取其频率，否则 0.0 |
| **Layer 3 — 目标论文** | 3 | 包含目标关键词的论文 | ≤max_nodes | 0.0（尚未阅读） |

**Layer 1 的关键词是如何找到的**（`graph_query.py:123-172` `get_keyword_cluster()`）：

1. 将 `target_topic` 标准化为 `kw_{topic}` 格式
2. 在倒排索引 `_kw_paper_idx` 中查找包含该关键词的所有论文
3. 遍历这些论文，统计它们携带的其他关键词的出现次数（共现频率）
4. 按共现频率降序排列，取前 k=5 个作为 "related_keywords"
5. 同时取前 k 篇包含目标关键词的论文作为 "papers"

$$\text{co\_occurrence}(kw_i, kw_{target}) = \sum_{p \in papers(kw_{target})} \mathbb{1}[kw_i \in keywords(p)]$$

### 掌握度传播算法

`KnowledgePropagation`（`propagation.py:16-227`）维护一个全局的 `mastery_state: Dict[str, float]`，记录每个 KG 节点的当前掌握度。

#### 传播公式

当一个学习事件发生时（用户阅读一篇论文），掌握度沿 KG 边多跳传播：

```
mastery(event_node) += delta × event_multiplier          ← 直接更新

for hop in 1..max_hops:
    new_delta = delta × decay^(hop) × rel_weight × edge_weight
    mastery(neighbor) += new_delta                       ← 邻居传播
```

**传播边类型及权重**（`propagation.py:187-191`）：

| 边类型 | 方向 | 权重 | 含义 |
|--------|------|------|------|
| `has_keyword` | 论文 → 关键词 | **0.8** | 读一篇论文，对其关键词的掌握提升最多 |
| `cite` | 论文 → 被引论文 | 0.4 | 读一篇论文，对其引用的前置工作有一定了解 |
| `publish_in` | 论文 → 期刊/会议 | 0.3 | 读一篇论文，对发表场馆有微弱认知 |

**衰减机制**：

- `decay = 0.6`：每传播一跳，影响力衰减 40%
- `max_hops = 3`：最多传播三跳
- `min_delta = 1e-4`：低于此阈值停止传播，避免无限扩散

$$\Delta mastery(B) = \delta_0 \times 0.6^{hop} \times w_{rel} \times w_{edge}$$

#### 批量初始化：时间衰减权重

`batch_update(history)`（`propagation.py:87-106`）在加载学习路径时被调用。关键设计：

```python
time_weight = (i + 1) / n    # 第 i 篇论文的权重 = 位置/总数
```

- 最早读的论文（i=0, weight ≈ 1/n）：掌握度贡献最小，模拟遗忘
- 最近读的论文（i=n-1, weight = 1.0）：掌握度贡献最大
- 中间论文线性插值

### 具体例子

假设用户 `student_wang`，历史阅读 3 篇 NLP 论文，目标学习方向是 `"reinforcement_learning"`。

#### 场景设定

**用户历史（按时间顺序，越后越近）**：

| 序号 | 论文 | 关键词 |
|------|------|--------|
| 0 (最早) | `paper_transformer` "Attention Is All You Need" | `kw_attention`, `kw_transformer`, `kw_nlp` |
| 1 | `paper_bert` "BERT" | `kw_transformer`, `kw_pretraining`, `kw_nlp` |
| 2 (最晚) | `paper_gpt` "GPT-3" | `kw_transformer`, `kw_pretraining`, `kw_language_model` |

**目标主题**：`reinforcement_learning`

**知识图谱中该主题的共现关键词**（`get_keyword_cluster` 返回）：

| 相关关键词 | 共现次数 | 含义 |
|-----------|---------|------|
| `kw_mdp` | 8 | Markov Decision Process |
| `kw_q_learning` | 7 | Q-Learning |
| `kw_policy_gradient` | 6 | Policy Gradient |
| `kw_deep_rl` | 5 | Deep RL |
| `kw_exploration` | 4 | Exploration strategies |

**目标论文**：`paper_dqn` "Playing Atari with Deep RL"

---

#### 第一步：生成四层节点

**Layer 0 — 已掌握关键词**（`_extract_known_keywords`）：

遍历用户历史 3 篇论文，统计每个关键词出现次数：

```
kw_transformer:    出现在 paper_transformer, paper_bert, paper_gpt → 3次
kw_nlp:            出现在 paper_transformer, paper_bert → 2次
kw_pretraining:    出现在 paper_bert, paper_gpt → 2次
kw_attention:      出现在 paper_transformer → 1次
kw_language_model: 出现在 paper_gpt → 1次
```

归一化（除以总论文数 3），取前 5：

| 节点 | 掌握度 | 计算 |
|------|--------|------|
| `kw_transformer` | 0.67 | 2/3 |
| `kw_nlp` | 0.67 | 2/3（并列，取前5都会进入） |
| `kw_pretraining` | 0.67 | 2/3 |
| `kw_attention` | 0.33 | 1/3 |
| `kw_language_model` | 0.33 | 1/3 |

**Layer 1 — 相关关键词**（来自 `get_keyword_cluster`）：

| 节点 | 初始掌握度 | 原因 |
|------|-----------|------|
| `kw_mdp` | 0.0 | 用户从未接触过 RL 领域 |
| `kw_q_learning` | 0.0 | 同上 |
| `kw_policy_gradient` | 0.0 | 同上 |
| `kw_deep_rl` | 0.0 | 同上 |
| `kw_exploration` | 0.0 | 同上 |

**Layer 2 — 目标关键词**：

| 节点 | 初始掌握度 |
|------|-----------|
| `kw_reinforcement_learning` | 0.0 |

**Layer 3 — 目标论文**：

| 节点 | 初始掌握度 |
|------|-----------|
| `paper_dqn` | 0.0 |

此时四层节点共 5+5+1+1 = 12 个，Layer 0 有值，Layer 1-3 全部为 0。

---

#### 第二步：掌握度传播（batch_update）

`KnowledgePropagation.batch_update(history)` 按时间顺序对每篇论文执行传播。

**传播 paper_transformer（i=0，time_weight = 1/3）**：

```
delta_0 = 1.0 × 1/3 = 0.333

① 直接更新 paper_transformer 自身:
   mastery(paper_transformer) = min(1.0, 0 + 0.333) = 0.333

② 沿 has_keyword 边传播（rel_weight=0.8，decay=0.6）:
   → kw_attention:    0.333 × 0.6¹ × 0.8 × 1.0 = 0.160
   → kw_transformer:  0.333 × 0.6¹ × 0.8 × 1.0 = 0.160
   → kw_nlp:          0.333 × 0.6¹ × 0.8 × 1.0 = 0.160

③ 沿 cite 边传播（rel_weight=0.4）:
   → 假设 paper_transformer 引用了 paper_elmo:
      0.333 × 0.6¹ × 0.4 × 1.0 = 0.080

④ 二阶传播（从 kw_attention 继续，decay²=0.36）:
   kw_attention 的邻居论文: 0.160 × 0.6 × 0.8 × 1.0 = 0.077
   ...（继续衰减直到 < min_delta 或 hop >= max_hops）
```

**传播 paper_bert（i=1，time_weight = 2/3）**：

```
delta_1 = 1.0 × 2/3 = 0.667

① mastery(paper_bert) += 0.667

② 沿 has_keyword:
   → kw_transformer:  += 0.667 × 0.6 × 0.8 × 1.0 = 0.320
   → kw_pretraining:  += 0.667 × 0.6 × 0.8 × 1.0 = 0.320
   → kw_nlp:          += 0.667 × 0.6 × 0.8 × 1.0 = 0.320
```

**传播 paper_gpt（i=2，time_weight = 3/3 = 1.0）**：

```
delta_2 = 1.0

① mastery(paper_gpt) += 1.0

② 沿 has_keyword:
   → kw_transformer:      += 1.0 × 0.6 × 0.8 × 1.0 = 0.480
   → kw_pretraining:      += 1.0 × 0.6 × 0.8 × 1.0 = 0.480
   → kw_language_model:   += 1.0 × 0.6 × 0.8 × 1.0 = 0.480
```

---

#### 第三步：传播后的掌握度汇总

三篇论文的传播效果累加（只列出在路径节点中的）：

| 节点 | paper_transformer | paper_bert | paper_gpt | **累计** | **截断(≤1.0)** |
|------|:---:|:---:|:---:|------|------|
| `kw_transformer` | 0.160 | 0.320 | 0.480 | 0.960 | **0.960** |
| `kw_pretraining` | 0 | 0.320 | 0.480 | 0.800 | **0.800** |
| `kw_nlp` | 0.160 | 0.320 | 0 | 0.480 | **0.480** |
| `kw_attention` | 0.160 | 0 | 0 | 0.160 | **0.160** |
| `kw_language_model` | 0 | 0 | 0.480 | 0.480 | **0.480** |
| `kw_mdp` | 0 | 0 | 0 | 0 | **0.000** |
| `kw_q_learning` | 0 | 0 | 0 | 0 | **0.000** |
| `paper_dqn` | 0 | 0 | 0 | 0 | **0.000** |

> 注：此例中 NLP 和 RL 领域之间没有跨领域的 KG 边，所以传播停留在 NLP 子图中。如果知识图谱中存在 `kw_transformer → cite → paper_decision_transformer`（一篇用 Transformer 做 RL 的论文），则传播会跨到 RL 领域——这正是 KG 传播的价值。

---

#### 第四步：应用到前端可视化

`KnowledgePropagation.apply_to_path()` 将上述 mastery 值写入每个 `PathNode.mastery`，然后：

**颜色映射**（`_mastery_to_color`）：
```
kw_transformer (0.960) →  #12B573  绿色（接近掌握）
kw_pretraining (0.800) →  #2AB87E  绿色偏黄
kw_nlp         (0.480) →  #E08E10  橙色（学习中）
kw_attention   (0.160) →  #6479E2  蓝紫色（刚接触）
kw_mdp         (0.000) →  #3B82F6  蓝色（未学习）
paper_dqn      (0.000) →  #3B82F6  蓝色（未学习）
```

**发光强度**：直接使用 mastery 值，`kw_transformer` 发光 0.96，`paper_dqn` 发光 0.0。

**三层渐变逻辑**（`propagation.py:213-226`）：
```
mastery 0.0 → 蓝色 #3B82F6（未学习）
mastery 0.5 → 橙色 #F59E0B（学习中，过渡态）
mastery 1.0 → 绿色 #10B981（已掌握）
```

---

### 算法特点与局限

**特点**：
- 掌握度传播是**有向衰减**的：一跳邻居获得 60%×权重，二跳 36%，三跳 22%，自然模拟了"读一篇论文，最直接学会的是它的关键词，间接了解它的引用文献"
- 时间衰减权重是**线性**而非指数的：越晚读的论文贡献越大（`(i+1)/n`），模拟的是近期记忆更清晰
- 掌握度全局累积、有上限（1.0），防止反复阅读同一领域论文导致值无限增长

**局限**：
- 当前 `_propagate` 只沿**出边**方向传播（`edge.dst_id`），不沿入边反向传播
- 传播仅依赖 KG 拓扑结构，未结合论文的实际文本内容或用户行为反馈强度
- `batch_update` 假定用户已完整阅读了历史中的每篇论文（全部以 `read` 事件处理）

---

## Q40: 私信（实时聊天）功能是如何从代码层面实现的？（2026-06-07）（git: 5ab8357）

### 相关文件

| 层 | 文件 | 职责 |
|----|------|------|
| 后端 REST | `controller/PrivateMessageController.java` | 6 个 REST 端点 |
| 后端 WebSocket | `controller/MessageWebSocketController.java` | STOMP 消息处理 |
| 后端服务 | `service/impl/PrivateMessageServiceImpl.java` | 核心业务逻辑 |
| 后端实体 | `entity/PrivateMessage.java`, `entity/UserContact.java` | 两张数据库表映射 |
| 后端配置 | `config/WebSocketConfig.java` | STOMP Broker 配置 |
| 前端页面 | `views/RealtimeChat.vue` | 主聊天页面 (~340 行) |
| 前端组件 | `components/chat/ConversationRail.vue` | 左侧会话面板 |
| 前端 API | `api/message.js` | 6 个 API 调用封装 |
| 数据库 | `private_messages`, `user_contacts` 表 | 消息存储 + 联系人关系 |

### 整体架构：双通道模式

```
┌─────────────────────────────────────────────────────┐
│  Vue 3 Frontend                                     │
│                                                      │
│  ┌──────────────┐    ┌───────────────────────────┐  │
│  │ REST 通道     │    │ WebSocket 通道 (STOMP)     │  │
│  │ axios 请求    │    │ SockJS → STOMP Client     │  │
│  │              │    │                            │  │
│  │ 发送/历史/列表 │    │ 实时推送 / 在线状态         │  │
│  └──────┬───────┘    └──────────┬────────────────┘  │
└─────────┼──────────────────────┼────────────────────┘
          │                      │
          ▼                      ▼
┌─────────────────────────────────────────────────────┐
│  Spring Boot Backend                                │
│                                                      │
│  PrivateMessageController  MessageWebSocketController│
│  (REST @RequestMapping)     (@MessageMapping STOMP)  │
│         │                         │                  │
│         └─────────┬───────────────┘                  │
│                   ▼                                   │
│        PrivateMessageServiceImpl                     │
│        (统一业务逻辑)                                  │
│                   │                                   │
│          ┌────────┴────────┐                         │
│          ▼                 ▼                          │
│   PrivateMessageMapper  UserContactMapper            │
│   (MyBatis-Plus)        (MyBatis-Plus)               │
└─────────────────────────────────────────────────────┘
          │                 │
          ▼                 ▼
   ┌──────────┐    ┌──────────────┐
   │ private_ │    │ user_contacts│
   │ messages │    │              │
   └──────────┘    └──────────────┘
```

### 数据模型（两张表）

**`private_messages`** — 消息本体：

| 字段 | 类型 | 含义 |
|------|------|------|
| `id` | BIGINT PK | 消息 ID |
| `sender_id` | BIGINT FK→user | 发送者 |
| `receiver_id` | BIGINT FK→user | 接收者 |
| `content` | TEXT | 消息内容 |
| `msg_type` | INT | 1=文本, 2=图片, 3=链接 |
| `is_read` | BOOLEAN | 是否已读 |
| `read_time` | DATETIME | 阅读时间 |
| `status` | INT | 0=已撤回, 1=正常 |
| `create_time` | DATETIME | 发送时间 |

**`user_contacts`** — 联系人关系：

| 字段 | 类型 | 含义 |
|------|------|------|
| `id` | BIGINT PK | 关系 ID |
| `user_id` | BIGINT FK→user | 用户 |
| `contact_id` | BIGINT FK→user | 联系人 |
| `relation_type` | VARCHAR | FOLLOW / FRIEND / COLLABORATOR |
| `create_time` | DATETIME | 建立时间 |

> 关键设计：联系人关系是**双向存储**的——A 给 B 发消息后，`user_contacts` 中同时插入 (A, B) 和 (B, A) 两条记录。这样查询"我的会话列表"只需 `WHERE user_id = ?`，无需 OR 查询。

Both mappers (`PrivateMessageMapper.java`, `UserContactMapper.java`) 都是空的 MyBatis-Plus `BaseMapper<T>` 接口——所有 CRUD 通过 MyBatis-Plus 内置方法完成，不需要手写 SQL。

---

### 消息发送的完整链路

以用户 A（id=1）向用户 B（id=2）发送 "Hello" 为例：

#### 第一步：前端输入 → 两级发送

`RealtimeChat.vue:306-338` `sendMessage()`：

```javascript
// 1. 乐观更新：消息立即出现在本地 UI
messages[selected].push({
  id: `tmp_${Date.now()}`,
  from: meId,           // 1
  to: selected.value,   // 2
  content: 'Hello',
  time: new Date().toLocaleTimeString(),
  _tmp: true            // 标记为临时消息
})

// 2. REST 持久化（确保消息一定存入数据库）
await sendMessageRest(2, 'Hello')
// → POST /api/message/send?receiverId=2&content=Hello

// 3. WebSocket 实时推送（让接收方立刻收到）
if (stompClient && connected.value) {
  stompClient.publish({
    destination: '/app/send-private',
    body: JSON.stringify({ token, receiverId: '2', content: 'Hello' })
  })
}
```

> 乐观更新失败处理：如果 REST 调用抛异常，临时消息被 splice 删除，ElMessage 提示错误。

#### 第二步：后端 REST 路径

`PrivateMessageController.java:45-49` → `PrivateMessageServiceImpl.sendMessage()`：

```java
// ① 插入消息
PrivateMessage message = new PrivateMessage();
message.setSenderId(1L);
message.setReceiverId(2L);
message.setContent("Hello");
message.setMsgType(1);    // 文本
message.setCreateTime(LocalDateTime.now());
privateMessageMapper.insert(message);

// ② 维护双向联系人关系
updateContact(1L, 2L);   // A 的联系人列表里有 B
updateContact(2L, 1L);   // B 的联系人列表里有 A
```

`updateContact()` 逻辑（`PrivateMessageServiceImpl.java:180-193`）：

```java
// 查询 (userId, contactId) 是否已存在
UserContact contact = userContactMapper.selectOne(queryWrapper);
if (contact == null) {
    // 不存在 → 插入新记录
    contact = new UserContact();
    contact.setUserId(userId);
    contact.setContactId(contactId);
    contact.setCreateTime(LocalDateTime.now());
    userContactMapper.insert(contact);
}
// 已存在 → 不重复插入（后续可扩展更新 last_message_time）
```

#### 第三步：后端 WebSocket 路径

`MessageWebSocketController.java:28-49` `handlePrivateMessage()`：

```java
@MessageMapping("/send-private")  // 对应前端 destination /app/send-private
public void handlePrivateMessage(@Payload Map<String, Object> messageData) {
    // ① 从 STOMP 消息体中提取 JWT（不是 HTTP Header）
    String token = (String) messageData.get("token");
    jwtUtil.validateToken(token);                    // 验证
    Long senderId = jwtUtil.getUserIdFromToken(token); // 解析用户 ID

    // ② 持久化到数据库（与 REST 路径调用同一个方法）
    privateMessageService.sendMessage(senderId, receiverId, content);

    // ③ 推送到接收者的私人队列
    messagingTemplate.convertAndSendToUser(
        receiverId.toString(),   // "2"
        "/queue/private",        // → /user/2/queue/private
        messageData              // 原始消息体转发
    );
}
```

#### 第四步：接收方前端接收

`RealtimeChat.vue:260-261` — WebSocket 连接时订阅：

```javascript
client.subscribe('/user/queue/private', msg => {
    const d = JSON.parse(msg.body)
    handleIncoming(d)
})
```

`handleIncoming()`（line 284-304）：

```javascript
function handleIncoming(data) {
    const from = Number(data.senderId)
    const to = Number(data.receiverId)
    const other = (from === meId) ? to : from  // 对方 ID

    // 追加到对应会话的消息列表
    if (!messages[other]) messages[other] = []
    messages[other].push({ id, from, to, content, time, isRead })

    // 更新联系人未读计数
    const contact = contacts.value.find(c => c.id == other)
    if (contact) {
        if (from !== meId) contact.unreadCount++
        contact.lastMessage = msg.content
    } else {
        // 从未聊过的新联系人 → 插入联系人列表顶部
        contacts.value.unshift({
            id: other, name: `用户${other}`,
            unreadCount: 1, lastMessage: msg.content
        })
    }
}
```

---

### 会话列表加载

`RealtimeChat.vue:99-122` → `GET /api/message/conversations`：

`PrivateMessageServiceImpl.getUserConversations()`（line 86-118）：

```java
// ① 查询 user_contacts WHERE user_id = ? → 得到所有联系人
List<UserContact> contacts = userContactMapper.selectList(queryWrapper);

// ② 对每个联系人，填充三样东西：
for (UserContact contact : contacts) {
    // a. 联系人 User 对象（头像、昵称）
    contact.setContact(userMapper.selectById(contact.getContactId()));

    // b. 未读消息数
    // SELECT count(*) FROM private_messages
    // WHERE sender_id=contact AND receiver_id=me AND is_read=false
    contact.setUnreadCount(privateMessageMapper.selectCount(unreadQuery));

    // c. 最后一条消息内容
    // SELECT * FROM private_messages
    // WHERE (sender=me AND receiver=contact) OR (sender=contact AND receiver=me)
    // ORDER BY create_time DESC LIMIT 1
    contact.setLastMessage(lastMessages.get(0).getContent());
}
```

### 聊天历史加载

选中联系人 → `GET /api/message/chat/{contactId}`：

```java
// PrivateMessageServiceImpl.java:66-83
QueryWrapper<PrivateMessage> queryWrapper = new QueryWrapper<>();
queryWrapper.and(wrapper -> wrapper
    .eq("sender_id", userId).eq("receiver_id", contactId)
    .or()
    .eq("sender_id", contactId).eq("receiver_id", userId)
).orderByAsc("create_time");

List<PrivateMessage> messages = privateMessageMapper.selectList(queryWrapper);

// 填充 sender/receiver User 对象（MyBatis @TableField(exist=false) 非 DB 字段）
for (PrivateMessage message : messages) {
    message.setSender(userMapper.selectById(message.getSenderId()));
    message.setReceiver(userMapper.selectById(message.getReceiverId()));
}
```

前端加载后自动标记对方发来的未读消息为已读（`RealtimeChat.vue:200-203`）：

```javascript
for (const msg of messages[id]) {
    if (msg.to === meId && !msg.isRead) {
        await markMessageRead(msg.id)  // POST /api/message/mark-read/{messageId}
        msg.isRead = true
    }
}
```

---

### 在线状态广播

`RealtimeChat.vue:268` — WebSocket 连接成功后：

```javascript
// 发布上线事件
client.publish({
    destination: '/app/user-online',
    body: JSON.stringify({ token })
})
```

`MessageWebSocketController.java:76-89`：

```java
@MessageMapping("/user-online")
public void handleUserOnline(@Payload Map<String, Object> data) {
    // 验证 JWT → 获取 userId
    Long userId = jwtUtil.getUserIdFromToken(token);

    // 广播到 /topic/user-status（所有订阅者都收到）
    messagingTemplate.convertAndSend(
        "/topic/user-status",
        Map.of("userId", userId, "status", "online")
    );
}
```

前端订阅（`RealtimeChat.vue:262-266`）：

```javascript
client.subscribe('/topic/user-status', msg => {
    const d = JSON.parse(msg.body)
    if (d.status === 'online') onlineSet.value.add(Number(d.userId))
    else onlineSet.value.delete(Number(d.userId))
})
```

`ConversationRail.vue` 中用 `onlineSet` 显示"在线协作中"/"异步协作中"标签。

---

### 合作者推荐算法

`PrivateMessageServiceImpl.getRecommendedCollaborators()`（line 121-171）：

```
前提条件: 当前用户角色必须是 RESEARCHER，否则直接返回空列表

1. 从 user_interest_history 解析当前用户的兴趣标签 → Set<String> myTags
2. 排除: 自己 + user_contacts 中已有的联系人
3. 查询所有 RESEARCHER 角色的其他用户
4. 对每个用户:
   a. 解析其 user_interest_history 兴趣标签 → Set<String> theirTags
   b. 计算交集: common = myTags ∩ theirTags
5. 过滤: 只保留 overlap > 0 的用户
6. 按 overlap 降序排列
7. 取前 2 个 → 构造 CollaboratorRecommendation 对象
```

示例：
```
myTags    = {"NLP", "Transformer", "Deep Learning"}
user_B    = {"NLP", "Transformer", "BERT"}          → overlap=2 ✓
user_C    = {"RL", "MDP"}                            → overlap=0 ✗
user_D    = {"NLP", "Deep Learning", "Attention"}    → overlap=2 ✓ (并列)

返回: [user_B(overlap=2), user_D(overlap=2)]
```

前端展示在 `ConversationRail.vue` 的推荐面板中，点击"开始对话"自动发送带共同兴趣标签的问候语：

```javascript
// RealtimeChat.vue:170
const greeting = `你好！我是${username}，发现我们在${commonInterests.join('、')}方面有共同兴趣，希望可以一起交流探讨！`
await sendMessageRest(rec.userId, greeting)
```

---

### STOMP Broker 配置

`WebSocketConfig.java` — 三行关键配置：

```java
@Configuration
@EnableWebSocketMessageBroker
public class WebSocketConfig implements WebSocketMessageBrokerConfigurer {

    public void configureMessageBroker(MessageBrokerRegistry config) {
        config.enableSimpleBroker("/topic", "/queue");
        // /topic → 广播（用户状态）
        // /queue → 点对点（私信，通过 /user/{id}/queue/... 路由）

        config.setApplicationDestinationPrefixes("/app");
        // 前端发送到 /app/xxx → 后端 @MessageMapping("xxx") 接收
    }

    public void registerStompEndpoints(StompEndpointRegistry registry) {
        registry.addEndpoint("/ws-messages")
                .setAllowedOriginPatterns("*")
                .withSockJS();  // SockJS 兼容层（自动降级非 WebSocket 环境）
    }
}
```

路由映射：

| 前端 STOMP destination | 后端处理方法 | 功能 |
|------------------------|-------------|------|
| `/app/send-private` | `handlePrivateMessage()` | 发送私信 |
| `/app/mark-read` | `handleMarkAsRead()` | 已读回执 |
| `/app/user-online` | `handleUserOnline()` | 上线广播 |
| `/user/queue/private` | （客户端订阅） | 接收私信推送 |
| `/topic/user-status` | （客户端订阅） | 接收在线状态变更 |

---

### JWT 在 WebSocket 中的特殊处理

HTTP 请求中 JWT 在 `Authorization: Bearer xxx` 头，但 **SockJS 不支持自定义 STOMP 头**。因此：

- JWT 放在 STOMP **消息体**中传输：`{ token, receiverId, content }`
- `MessageWebSocketController` 的每个 `@MessageMapping` 方法都自行从 `messageData.get("token")` 提取并验证
- `/ws-messages/**` 在 `SecurityConfig` 白名单中（line 60），不走 HTTP JWT 过滤器
- 前端每个 STOMP `publish()` 都在 body 中附带 `token`

---

### 前端降级策略

`RealtimeChat.vue:99-122` `loadConversations()` 的 fallback 逻辑：

```
① 尝试 REST 获取真实联系人列表
   ↓ 失败或为空
② 尝试从 /api/knowledge/graph 获取知识图谱节点作为展示
   ↓ 标记 isRealUser=false，禁止发送消息
   ↓ 失败
③ 显示"还没有协作会话"
```

`RealtimeChat.vue:217-282` `connect()` 的 fallback 逻辑：

```javascript
// ① 优先 import 本地 npm 包 (sockjs-client, @stomp/stompjs)
//    ↓ 失败
// ② CDN 加载 UMD 脚本
//    ↓ 失败
// ③ console.debug（静默降级，用户仍可通过 REST 发送消息）
```

---

### 关键设计决策总结

| 决策 | 做法 | 原因 |
|------|------|------|
| 双通道 | REST + WebSocket 并行 | REST 保证持久化可靠性，WebSocket 保证实时性 |
| 乐观更新 | 消息先显示再持久化 | 消除输入延迟感；失败时 splice 回滚 |
| 联系人双向存储 | (A,B) + (B,A) 两条记录 | 查询简化：`WHERE user_id=?` 替代 OR 查询 |
| JWT 在消息体 | 不放 HTTP Header | SockJS 不支持自定义 STOMP CONNECT 头 |
| 所有 @MessageMapping 自行验 JWT | 无全局 STOMP 拦截器 | 每条消息独立验证，安全性与 REST 等价 |
| REST 返回裸实体 | 不用 `Result` 信封 | 前端 `res.data \|\| res` 兼容处理；历史原因 |
| 空白 MyBatis-Plus Mapper | 不写 SQL | 单表 CRUD 完全由 BaseMapper 内置方法覆盖 |
| CDN fallback | sockjs + stomp CDN | 网络受限环境下 npm 包可能加载失败 |