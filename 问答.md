# 科研推荐系统答辩问答

以下内容站在答辩老师可能提问的角度整理，回答尽量兼顾专业性、完整性和可理解性，便于现场展开说明。

---

## 1. 你这个项目的整体架构是什么？为什么要这样拆分？

**回答：**

本项目采用的是**前后端分离 + 推荐微服务独立部署**的三层架构，整体上可以分为三个部分：

1. **前端（fronted）**
   - 基于 Vue 3 + Vite 实现。
   - 负责界面展示、用户交互、推荐结果展示、知识图谱可视化、社区与消息页面等。
   - 它本身不做复杂推荐计算，而是通过 Axios 调用后端接口。

2. **业务后端（backend）**
   - 基于 Spring Boot 实现，是整个系统的业务中枢。
   - 负责用户注册登录、JWT 鉴权、论文检索、行为日志记录、私信、社区接口，以及对 Python 推荐服务的统一调用。
   - 同时负责把数据库中的业务数据组织成前端可直接使用的接口格式。

3. **推荐微服务（rl-service）**
   - 基于 FastAPI + Python 实现。
   - 负责强化学习推荐、候选召回、知识图谱结构特征建模、推荐解释生成、模型训练与热更新。
   - 这一层和业务后端解耦，便于后续单独优化算法，而不影响 Java 业务系统。

这样拆分的主要原因有三点：

- **职责清晰**：Java 后端更适合做权限、事务、数据库和接口编排；Python 更适合做机器学习与模型训练。
- **便于迭代**：推荐算法升级时，只需要替换 Python 服务，不必改动整套业务系统。
- **更符合工程实践**：推荐系统往往是独立服务，因为它的计算特点、依赖库和部署节奏与普通 CRUD 系统不同。

---

## 2. 推荐结果是如何生成的？完整流程是什么？

**回答：**

推荐结果的生成可以概括为“**前端发起请求 -> Java 后端组织上下文 -> Python 服务完成推荐 -> 后端补全业务字段 -> 前端展示**”。

### 第一步：前端发起推荐请求

前端调用 `frontend/src/api/recommend.js` 中的 `getRecommendations()`，实际请求是：

- `GET /api/recommend?k=10`

Axios 的基础路径配置在 `frontend/src/utils/request.js` 中，`baseURL` 是 `/api`。同时它会自动从 `localStorage` 中读取 JWT，并放到请求头 `Authorization: Bearer ...`。

### 第二步：Spring Boot 后端接收请求

`backend/src/main/java/com/example/research/controller/RecommendController.java` 接收该请求。

控制器会做两件事：

1. 从 `Authentication` 中取出当前登录用户的 `userId`
2. 调用 `RecommendServiceImpl.getRecommendations(userId, k)`

### 第三步：后端构造推荐上下文

`RecommendServiceImpl` 不会直接“拍脑袋推荐”，而是先从数据库中查询用户近期行为：

- 读取 `behavior_log` 表中的交互记录
- 提取最近交互过的论文 ID
- 再根据本地 `paper` 表把这些数据库主键 ID 转换成 **AMiner ID**

这里要特别说明：  
系统内部数据库主要用的是本地 `paper.id`，但推荐服务跨模块通信时使用的是 **AMiner 论文标识**。这样做的意义是让推荐服务使用统一的学术数据标识，而不是依赖数据库自增主键。

### 第四步：后端调用 Python 推荐服务

Java 后端通过 `PythonRecClient` 使用 `RestTemplate` 调用：

- `POST http://localhost:8000/recommend`

发送的数据包括：

- `user_id`
- `k`
- `history`
- `strategy`

需要注意的是，Python 接口使用的是 **snake_case** 字段命名，所以 Java 里通过 `@JsonProperty` 做了字段映射。

### 第五步：Python 服务生成推荐结果

Python 侧在 `rl-service/services/recommendation_service.py` 中完成核心推荐流程，大致分为五步：

1. **特征构建**：用 `FeatureBuilder` 构造用户状态向量
2. **候选召回**：用 `CandidateGenerator` 产生候选论文集合
3. **强化学习排序**：用 `RLRanker + ActorCriticAgent` 对候选集排序
4. **解释生成**：用 `ExplanationGenerator` 生成推荐理由和置信度
5. **结果封装**：形成标准 JSON 响应返回给 Java 后端

### 第六步：Java 后端补全业务信息

Python 返回的是推荐候选结果，但其中的 `paper_id` 实际是 AMiner ID。  
Java 后端拿到这些结果后，会再去查本地数据库，把返回结果映射回真实的 `Paper` 对象，并补全：

- 本地数据库主键 `paperId`
- 标题
- 年份
- 期刊/会议
- 被引量等

如果某条 Python 返回的论文在本地数据库中不存在，则保留 Python 返回的字段作为兜底展示。

### 第七步：返回前端展示

后端最终返回统一的：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "userId": "1",
    "modelVersion": "...",
    "pythonServiceAvailable": true,
    "recommendations": [...]
  }
}
```

前端收到以后，在页面中展示论文标题、推荐理由、相似度、置信度等信息。

---

## 3. 如果让我一句话概括推荐模块的核心逻辑，你会怎么说？

**回答：**

可以概括为：

> **系统先利用用户历史行为构造用户状态，再对候选论文集合进行强化学习排序，并结合语义相似度、知识图谱结构信息和可解释理由，输出 Top-K 推荐结果。**

这句话基本把“输入是什么、模型做了什么、输出是什么”都交代清楚了。

---

## 4. 这个系统为什么不用传统协同过滤，而要用强化学习？

**回答：**

传统协同过滤更适合处理“用户-物品静态评分矩阵”的问题，但本项目是科研推荐场景，存在几个特点：

1. **用户兴趣是动态变化的**
   - 用户今天看强化学习，明天可能开始关注图神经网络。
   - 强化学习可以把推荐过程看成一个连续决策过程，而不是一次性打分。

2. **推荐有长期收益**
   - 有些论文不是“当下最容易点击”的，但对用户后续科研成长价值更高。
   - 强化学习中的奖励设计可以同时考虑即时反馈和长期收益。

3. **需要平衡探索与利用**
   - 如果一直只推荐用户已经熟悉的内容，会形成信息茧房。
   - Actor-Critic 中引入熵正则项，可以鼓励探索，提升推荐多样性。

因此，本项目采用强化学习，不只是为了“技术先进”，而是因为它更适合描述科研推荐这种**序列化、动态化、长期优化**的问题。

---

## 5. 你们的强化学习模型具体是怎么定义状态、动作和奖励的？

**回答：**

这是答辩里非常关键的问题，我一般会分三部分回答。

### 1. 状态（State）

状态向量由 `rl-service/features/feature_builder.py` 构建。

当前实现中，状态主要由三部分拼接而成：

1. **interest_vector**：用户兴趣向量  
2. **history_vector**：用户历史行为向量  
3. **kg_vector**：知识图谱结构特征向量（可选）

如果启用了知识图谱，状态维度是：

- `32 + 32 + 32 = 96` 维

如果不启用知识图谱，则是：

- `32 + 32 = 64` 维

从含义上说，这个状态表示的是“**用户当前的研究兴趣、历史阅读痕迹，以及其在学术知识网络中的位置**”。

### 2. 动作（Action）

动作的含义是：

> **从候选论文集合中选择一个推荐对象**

在配置中 `action_num=20`，表示一次决策面对的是一个离散候选动作空间。  
模型输出每个动作的策略概率，再从中选择推荐项。

### 3. 奖励（Reward）

奖励由 `rl-service/utils/reward.py` 和环境 `env/rec_env.py` 共同作用。

奖励综合考虑多个信号：

- 点击行为
- 收藏行为
- 阅读时长
- 主题匹配度
- 长期科研价值
- 知识图谱拓扑相关度

也就是说，它不是简单地“点了就加 1”，而是一个加权组合。  
这样做的目的是让模型不仅关注短期点击率，还关注推荐内容对科研成长的价值。

---

## 6. Actor-Critic 在这个项目中是如何工作的？

**回答：**

`rl-service/agent.py` 中实现了 Actor-Critic 智能体，其核心思想是把“选什么”和“选得好不好”拆成两个网络：

### 1. Actor（策略网络）

Actor 的作用是：

> 根据当前用户状态，输出每个候选动作的概率分布

也就是回答“**推荐哪篇论文更合适**”。

### 2. Critic（价值网络）

Critic 的作用是：

> 评估当前状态的价值，或者说判断这个状态下未来大概还能获得多少收益

也就是回答“**当前推荐决策的长期价值怎么样**”。

### 3. 更新方式

项目里采用的是 TD(0) 更新：

- TD 误差：`δ = r + γV(s') - V(s)`

然后：

- Critic 最小化 `δ²`
- Actor 最大化 `log π(a|s) * δ`

同时项目还加入了**熵正则项**，用于鼓励探索，避免模型过早收敛到非常单一的推荐模式。

从工程角度看，这种设计优点是：

- 相比纯策略梯度更稳定
- 相比纯价值方法更适合直接做动作选择
- 便于做“探索-利用平衡”

---

## 7. 模型训练流程是怎样的？

**回答：**

训练流程分为“**触发训练**”和“**训练内部循环**”两个层面。

### 一、外部触发流程

前端或后端调用：

- `POST /api/recommend/train`

Java 后端在 `RecommendController` 中接收后，会调用 `PythonRecClient.triggerTraining()`，继续请求 Python 服务的：

- `POST /train`

FastAPI 收到之后，会把训练任务放到后台执行，不阻塞主线程。

训练结束后会自动调用：

- `reload_model()`

实现模型热更新，也就是不需要重启推荐服务。

### 二、训练内部流程

训练主逻辑位于 `rl-service/train.py`：

1. 初始化配置 `Config`
2. 构建知识图谱嵌入器（如果启用 KG）
3. 初始化奖励函数
4. 初始化环境 `ResearchRecEnv`
5. 初始化智能体 `ActorCriticAgent`
6. 进入 episode 循环

每个 episode 的基本过程是：

1. `env.reset()`：重置环境，生成用户状态
2. `agent.select_action(state)`：根据当前策略选一个动作
3. `env.step(action)`：执行推荐动作，获得下一状态和奖励
4. `agent.update(...)`：根据 TD 误差更新 Actor 和 Critic
5. 重复直到 episode 结束

训练过程中，如果当前 episode 的总奖励优于历史最佳，就会调用：

- `agent.save_model()`

把模型保存到：

- `checkpoints/ac_model.pth`

这体现的是“**以最优奖励为标准进行模型持久化**”。

---

## 8. 这个训练是真实数据训练，还是模拟环境训练？

**回答：**

目前系统的训练设计属于**“真实工程接口 + 模拟环境驱动”的混合形态”**。

更准确地说：

1. **业务后端层面**
   - 用户行为日志 `behavior_log`
   - 论文数据 `paper`
   - 用户与消息数据
   这些都是走真实数据库 MySQL 的。

2. **强化学习训练层面**
   - 环境 `ResearchRecEnv` 目前仍以内置数据生成和模拟交互为主
   - `FeatureBuilder`、`CandidateGenerator` 都保留了与真实数据库、向量库对接的接口
   - `AMinerLoader` 支持加载真实 AMiner 数据；如果本地没有数据文件，则退回 mock 数据

也就是说，项目现在已经把训练链路、推荐链路、数据接口都搭好了，但训练环境还处于**可替换为真实学术数据的工程化原型阶段**。

这类回答要注意，不要说“全都是真实在线训练”，那不准确；也不要说“完全是假的”，那也不对。  
准确表述应该是：

> **业务系统是真实工程形态，强化学习训练环境当前是可替换的原型实现，已预留接入真实学术数据和向量检索能力的接口。**

---

## 9. 候选集是怎么生成的？为什么不是直接对全库排序？

**回答：**

系统没有直接对全库论文做强化学习打分，而是先召回候选集，再排序。这是推荐系统中非常常见的“两阶段架构”：

1. **召回（Recall）**
2. **排序（Ranking）**

候选召回由 `rl-service/recommender/candidate_generator.py` 负责，当前支持三种策略：

- **similarity**：基于用户兴趣向量与论文向量的相似度召回
- **popular**：按被引量召回热门论文
- **hybrid**：混合召回，兼顾相关性和热门度

这样设计有两个原因：

1. **效率更高**
   - 如果直接对海量论文逐一做复杂模型打分，成本太高
   - 先缩小到一个候选集，再做精排，是工业界常见做法

2. **推荐质量更稳定**
   - 召回阶段先保证“候选集大致相关”
   - 排序阶段再用更复杂的模型做精细决策

所以本项目实际上是：

> **候选召回负责找到“可能合适的论文”，强化学习排序负责在这些候选中决定“最该先推哪几篇”。**

---

## 10. 最终排序分数是怎么计算的？

**回答：**

排序逻辑在 `rl-service/recommender/ranker.py` 中实现。

它不是只看强化学习策略分，而是一个融合分数。当前权重大致为：

- Actor 策略分：0.5
- 语义相似度：0.3
- 知识图谱拓扑分：0.2

如果没有启用知识图谱，则退化为：

- Actor：0.6
- 语义相似度：0.4

这样做的好处是：

1. **策略分**体现模型对长期收益的判断
2. **语义相似度**保证推荐内容和用户兴趣不要偏得太远
3. **知识图谱分**增强学术结构关联性，比如引用关系、作者协作关系、领域邻近性

所以最终结果不是单一模型的黑箱输出，而是一个**融合型排序框架**。

---

## 11. 知识图谱在这个系统中起什么作用？

**回答：**

知识图谱的作用不是简单“画图好看”，而是为推荐和学习路径提供**结构化语义关系**。

在 `rl-service/knowledge_graph/kg_builder.py` 中，知识图谱建模了多种节点和边：

### 节点类型

- Paper（论文）
- Author（作者）
- Keyword（关键词）
- Venue（期刊/会议）

### 关系类型

- `author_of`
- `cite`
- `has_keyword`
- `publish_in`
- `co_author`

基于这些关系，系统可以回答很多传统向量模型不好直接表达的问题，例如：

- 这篇论文是不是来自用户熟悉的引用链？
- 它和用户常看的关键词子图是否相近？
- 它是否处于某个高影响力学术社群中？

然后 `kg_embedder.py` 会把这些结构特征编码成固定长度向量，供推荐模型使用。

所以知识图谱在本项目中的价值有三层：

1. **增强推荐表示能力**
2. **支持学习路径生成**
3. **提高结果解释性**

### 论文表述版（可直接插入正文）

在本研究中，知识图谱并不是独立于推荐模型之外的附属可视化模块，而是贯穿**数据组织、特征表示、候选召回、排序决策与结果解释**的核心语义基础设施。系统将论文、作者、关键词、期刊/会议等学术对象统一建模为异构图，并用引用关系、作者-论文关系、论文-关键词关系、论文-发表场所关系以及作者合作关系描述它们之间的结构联系。形式化地说，可将该知识图谱表示为异构图 \(G=(V,E)\)，其中 \(V\) 包含 `Paper`、`Author`、`Keyword`、`Venue` 等多类型节点，\(E\) 包含 `cite`、`author_of`、`has_keyword`、`publish_in`、`co_author` 等多类型边。

知识图谱首先在**数据一致性层**发挥作用。系统以 AMiner 论文标识作为跨服务统一标识，在图数据库中维护论文主数据和学术关系网络，在 MySQL 中保留用户、权限、行为日志、社区、消息等事务型业务数据。这样做的意义在于：推荐微服务返回的论文不再依赖“必须预先插入 MySQL 才能被业务系统识别”的前提，而是可以先在图数据库中完成统一建模，再由后端按需同步为 MySQL 影子论文记录，从而保证推荐结果、论文详情、搜索与行为记录之间的标识一致性和链路闭环。

知识图谱其次在**状态表示层**发挥作用。系统并不是只用用户历史点击或语义向量来表示用户，而是进一步提取用户在知识图谱中的结构位置。具体而言，`kg_embedder.py` 会围绕论文节点计算一组结构化特征，例如被引入度、引用出度、关键词连通范围、作者活跃度、场所影响力、合著网络密度、引用拓扑深度以及时间新颖度等；随后，对用户历史交互论文的图结构表示进行聚合，形成用户级知识图谱向量 `kg_vector`。在启用知识图谱时，强化学习状态向量由 `interest_vector`、`history_vector` 与 `kg_vector` 三部分拼接构成，即：

\[
s_t = [\,interest\_vector;\ history\_vector;\ kg\_vector\,]
\]

这意味着模型学习到的不是“用户喜欢哪些词”这一层面的浅层兴趣，而是“用户位于哪一类学术主题子图、倾向于哪些引用链与作者合作网络”这一层面的结构化学术偏好。

再次，知识图谱在**候选召回与排序决策层**提供了重要的结构约束。语义相似度只能回答“内容像不像”，而知识图谱能够回答“学术关系近不近”。例如，两篇论文即使文本表达不同，只要位于相近的关键词簇、引用链或作者社群中，仍可能对同一用户具有较高相关性。系统因此将知识图谱拓扑相关度作为排序融合因素之一，与 Actor 策略分和语义相似度共同决定最终推荐顺序。这使推荐结果不仅具备文本层面的相似性，还具备学术演化路径上的连续性与合理性，更符合科研场景中“从基础到前沿、从经典到扩展”的认知规律。

最后，知识图谱在**结果解释与学习路径生成层**体现出明显优势。由于推荐对象与用户历史之间存在显式的图关系，系统可以将“共享关键词”“位于同一引用链”“来自相近作者合作网络”“属于同一高影响力研究社区”等关系直接转化为可解释理由，而不是仅给出难以追溯的黑箱分数。同样，在学习路径任务中，系统也可以利用图上的最短路径、主题邻接关系与论文依赖结构，组织出从基础概念到目标主题、再到代表性论文的渐进式学习路线。

因此，知识图谱在本系统中的作用可以概括为：**在数据层统一学术对象，在表示层补充结构特征，在决策层增强关系建模，在解释层提供可追溯证据。** 它并不是替代强化学习模型，而是为强化学习提供更符合科研知识组织方式的状态空间与关系先验，从而提升推荐结果的一致性、可解释性和学术语义质量。

---

## 12. 推荐解释是怎么生成的？为什么说它有可解释性？

**回答：**

推荐解释由 `rl-service/recommender/explain.py` 负责。

它不是让大模型“随便编一个理由”，而是基于结构化特征生成解释，比如：

- 相似度高
- 主题重叠
- 被引量高
- 论文较新
- 与历史阅读相关

然后系统把这些真实可追溯的特征，转化为自然语言说明，例如：

- “与你在强化学习方向的研究高度相关”
- “高被引经典论文，学术影响力强”
- “2024 年发表的最新研究，紧跟领域前沿”

所以它的可解释性主要体现在：

1. **理由来源真实**
2. **和推荐分数相关**
3. **可以被用户理解**

这在科研推荐场景尤其重要，因为用户往往不是只关心“给了我什么”，而更关心“为什么给我这个”。

---

## 13. 如果 Python 推荐服务挂掉了，系统会怎么样？

**回答：**

系统设计了**降级机制**，不会因为推荐微服务不可用就导致整个系统崩溃。

在 `RecommendServiceImpl` 中，如果调用 `PythonRecClient.getRecommendations()` 失败，系统会自动进入 fallback 流程，返回：

> **按高被引排序的热门论文推荐**

这说明系统做了可用性设计：

- 推荐服务可增强系统智能性
- 但不是业务系统的单点故障源

从答辩角度，这一点可以体现你对工程可靠性的考虑，而不是只关注模型本身。

---

## 14. 用户行为日志在推荐系统中扮演什么角色？

**回答：**

用户行为日志是推荐系统的重要输入，它记录了用户与论文的交互过程。  
在本项目中，行为接口包括：

- `/api/behavior/click`
- `/api/behavior/favorite`
- `/api/behavior/read`

这些行为会被存入 `behavior_log` 表中，记录字段包括：

- 用户 ID
- 论文 ID
- 行为类型
- 阅读时长
- 来源页面
- 时间戳

它的作用主要有三类：

1. **构建用户历史上下文**
   - 推荐前先读取最近交互过的论文

2. **作为强化学习奖励设计的依据**
   - 点击、收藏、阅读时长都可映射为奖励信号

3. **为后续离线训练和评估提供数据基础**
   - 未来可以做更真实的训练样本构造

所以行为日志不是“顺手记一下”，而是连接业务交互与推荐算法的桥梁。

---

## 15. 这个项目中登录鉴权的流程是什么？

**回答：**

项目采用的是 **JWT 无状态认证**。

流程如下：

1. 用户调用 `/api/user/login`
2. `UserServiceImpl` 校验用户名密码
3. 如果成功，利用 `JwtUtil` 生成 Token
4. 前端把 Token 存到 `localStorage`
5. 后续 Axios 请求自动带上 `Authorization: Bearer xxx`
6. Spring Security 的 `JwtFilter` 解析 Token
7. 解析成功后，把 `userId` 放入 `Authentication.principal`

这也是为什么很多控制器里都能直接写：

```java
Long userId = (Long) authentication.getPrincipal();
```

这样做的优点是：

- 后端无 Session，更适合前后端分离
- 多服务扩展更方便
- 与移动端、Web 端都更容易对接

---

## 16. 实时私信模块的调用流程是什么？

**回答：**

实时私信模块采用的是 **SockJS + STOMP + Spring WebSocket**。

### 前端流程

前端在 `RealtimeChat.vue` 中：

1. 连接 `/ws-messages`
2. 订阅 `/user/queue/private`
3. 订阅 `/topic/user-status`
4. 发送消息时向 `/app/send-private` 发布消息

### 后端流程

后端在 `MessageWebSocketController.java` 中：

1. 接收 `/app/send-private`
2. 从消息体中解析 token，获取发送者身份
3. 调用 `PrivateMessageServiceImpl.sendMessage()` 持久化到数据库
4. 通过 `SimpMessagingTemplate.convertAndSendToUser()` 把消息推送给接收者

这个模块体现的是：

- **REST 适合查数据**
- **WebSocket 适合推数据**

两者结合，既能保证消息落库，又能保证实时交互体验。

---

## 17. 学习路径功能是怎么实现的？

**回答：**

学习路径功能位于 Python 服务中，入口是：

- `POST /learning-path`

核心实现文件是 `rl-service/learning_path/path_builder.py`。

逻辑可以概括为：

1. 根据用户历史阅读，识别其已经掌握的关键词
2. 在知识图谱中找到目标主题相关的关键词簇和目标论文
3. 按深度组织成“基础知识 -> 相关主题 -> 目标主题 -> 代表论文”的层级路径
4. 估算学习时长和覆盖率
5. 输出给前端进行三维可视化

因此它不是简单“推荐几篇论文”，而是在做一种**面向科研成长路径的结构化推荐**。

这部分很适合在答辩中强调项目的创新点，因为它把“推荐结果”提升成了“学习路线”。

---

## 18. 请你介绍一下各个微服务模块、目录和关键文件的作用。

**回答：**

这个问题建议按“前端、后端、推荐服务”三部分回答，而不是逐个文件机械背诵。

### 一、前端（frontend）

#### 1. 应用入口

- `src/main.js`：Vue 应用启动入口，注册 Pinia、Router、Element Plus
- `src/App.vue`：应用根组件

#### 2. 路由与全局访问控制

- `src/router/index.js`：定义页面路由，并做未登录跳转控制

#### 3. 接口访问层

- `src/utils/request.js`：Axios 封装，统一处理 baseURL、Token 注入、401 失效跳转
- `src/api/*.js`：按业务拆分接口，例如用户、推荐、消息、可视化等

#### 4. 页面层

- `src/views/Home.vue`：首页/推荐展示
- `src/views/Search.vue`：论文搜索
- `src/views/KnowledgeGraph.vue`：知识图谱展示
- `src/views/RealtimeChat.vue`：实时私信
- `src/views/Profile.vue`、`EditProfile.vue`：个人中心
- `src/views/Community.vue`：社区页面
- `src/views/Login.vue`：登录注册入口

#### 5. 状态管理

- `src/store/userStore.js`：用户 token 和个人资料管理

### 二、业务后端（backend）

#### 1. 启动与配置

- `ResearchApplication.java`：Spring Boot 启动类
- `config/SecurityConfig.java`：Spring Security + JWT 配置
- `config/AppConfig.java`：Python 服务专用 RestTemplate、全局 CORS
- `config/WebSocketConfig.java`：STOMP WebSocket 配置
- `config/GlobalExceptionHandler.java`：全局异常处理

#### 2. 控制层

- `controller/UserController.java`：注册、登录、个人信息
- `controller/PaperController.java`：论文列表、详情、搜索
- `controller/RecommendController.java`：推荐、训练触发、模型信息
- `controller/BehaviorController.java`：行为日志记录
- `controller/MessageWebSocketController.java`：实时消息
- `controller/KnowledgeController.java`：知识图谱接口（当前偏演示数据）
- `controller/VisualizationController.java`：可视化接口（当前偏演示数据）

#### 3. 服务层

- `service/impl/UserServiceImpl.java`：用户注册登录逻辑
- `service/impl/PaperServiceImpl.java`：论文查询逻辑
- `service/impl/RecommendServiceImpl.java`：推荐编排核心
- `service/impl/PrivateMessageServiceImpl.java`：私信持久化与会话列表逻辑

#### 4. 数据访问层

- `repository/*Mapper.java`：MyBatis-Plus 数据访问接口
- `entity/*.java`：数据库实体对象
- `dto/*.java`：接口收发对象

#### 5. 外部服务调用

- `client/PythonRecClient.java`：调用 Python 推荐服务的桥梁

#### 6. 工具层

- `util/Result.java`：统一返回结构
- `util/JwtUtil.java`：JWT 生成与解析

### 三、推荐微服务（rl-service）

#### 1. API 层

- `api/server.py`：FastAPI 入口，暴露推荐、训练、模型信息、健康检查、学习路径接口

#### 2. 服务编排层

- `services/recommendation_service.py`：推荐主流程编排

#### 3. 训练与模型

- `train.py`：模型训练主循环
- `agent.py`：Actor-Critic 智能体
- `models/actor.py`：策略网络
- `models/critic.py`：价值网络

#### 4. 环境与奖励

- `env/rec_env.py`：强化学习环境
- `utils/reward.py`：奖励函数
- `utils/logger.py`：训练日志记录

#### 5. 特征与推荐链路

- `features/feature_builder.py`：用户状态特征构建
- `recommender/candidate_generator.py`：候选召回
- `recommender/ranker.py`：强化学习排序
- `recommender/explain.py`：推荐解释生成

#### 6. 数据与知识图谱

- `dataset/aminer_loader.py`：AMiner 数据加载
- `knowledge_graph/kg_builder.py`：知识图谱构建
- `knowledge_graph/kg_embedder.py`：图谱结构向量化
- `learning_path/path_builder.py`：学习路径生成

#### 7. 全局配置

- `config.py`：模型、训练、维度、知识图谱等全局参数

如果老师问“是不是每个文件都独立完成业务”，你可以补一句：

> **不是孤立工作的，而是按“接口层—服务层—算法层—数据层”的方式协同完成推荐任务。**

---

## 19. 后端和推荐微服务之间的调用关系是怎样的？

**回答：**

后端和推荐微服务之间是典型的 **HTTP 同步调用 + 训练异步触发** 关系。

### 推荐调用

业务后端调用：

- Python `/recommend`

这是同步调用，因为前端要实时拿到推荐结果。

### 训练调用

业务后端调用：

- Python `/train`

这个是异步触发，因为训练时间长，不适合阻塞接口线程。

### 模型状态调用

业务后端还会调用：

- `/model/info`
- `/health`

用于判断服务可用性和模型状态。

因此，两者关系不是数据库共享，也不是消息队列驱动，而是：

> **Java 后端负责业务编排，Python 服务负责算法计算，二者通过清晰的 HTTP API 契约协作。**

---

## 20. 如果老师问“你的系统有哪些亮点”，你会怎么回答？

**回答：**

我会从四个角度回答：

### 1. 工程与算法结合

它不是单独的模型实验，而是完整的前后端系统，包含登录、搜索、推荐、消息、图谱、学习路径等完整业务链路。

### 2. 强化学习应用到科研推荐

不是简单做相似度检索，而是把推荐建模为连续决策问题，考虑长期收益和探索利用平衡。

### 3. 引入知识图谱增强推荐

不仅看文本相似度，还把论文、作者、关键词、引用关系组织成结构化图谱，提高学术语义表达能力。

### 4. 结果具备解释性和可扩展性

系统不仅告诉用户“推荐什么”，还能解释“为什么推荐”，并预留了真实 AMiner 数据、向量数据库和 LLM 改写接口，具备扩展空间。

---

## 21. 如果老师追问“目前系统还有哪些可以继续改进的地方”，该怎么回答？

**回答：**

这个问题不能回避，应该诚实但专业地回答。

我会说目前主要有四个可继续优化方向：

1. **训练环境进一步真实化**
   - 当前强化学习环境已具备完整接口，但还可以更深入接入真实行为序列与真实候选召回结果

2. **候选召回进一步工程化**
   - 当前保留了向量库和 Elasticsearch 接口，后续可以接入 Faiss、Milvus 或 ES 实现大规模召回

3. **评估体系更完善**
   - 后续可加入 Precision@K、Recall@K、NDCG、覆盖率、多样性等指标，形成更完整实验评估

4. **知识图谱与学习路径进一步联动**
   - 目前已经有学习路径生成能力，后续可让推荐结果与学习路径动态联动，实现“推荐-学习-反馈”的闭环

这样的回答体现的是：

- 你了解当前工作的边界
- 你也清楚未来的演进方向
- 你不是只会“做出来”，而是知道“下一步怎么做”

---

## 22. 如果老师最后问“请你用 1 分钟总结这个系统的核心贡献”，可以怎么回答？

**回答：**

我会这样总结：

> 本项目构建了一个面向科研场景的智能推荐系统，采用 Vue + Spring Boot + FastAPI 的分层架构，将业务系统与推荐算法解耦。系统通过记录用户行为日志，结合 Actor-Critic 强化学习、知识图谱结构特征和可解释推荐机制，为用户生成个性化论文推荐，并进一步扩展到学习路径生成与知识可视化。它的意义不只是完成了一个推荐模型，而是把科研推荐从“论文检索”提升到了“个性化发现与学习辅助”的层面。

---

## 23. 答辩时的表达建议

**回答：**

如果现场回答这些问题，建议遵循一个固定结构：

1. **先讲目标**
   - 这个模块是为了解决什么问题

2. **再讲流程**
   - 输入是什么，中间做了什么，输出是什么

3. **最后讲价值**
   - 为什么这样设计，而不是别的设计

例如讲推荐流程时，不要一上来就背文件名，而要先说：

> “推荐模块的目标是根据用户历史行为生成个性化论文推荐。流程上，先由后端提取历史行为，再调用 Python 推荐服务进行候选排序，最后回填数据库论文信息并返回前端展示。”

这样的回答会比“先背代码结构”更像答辩，而不是代码汇报。

---

## 24. 当前图数据库是如何被后端与推荐服务调用的？

**回答：**

当前实现采用的是**双库协同**模式，而不是“所有数据都只放一个库里”。

- **Neo4j**：存放论文主数据与知识关系网络
- **MySQL**：存放用户、权限、行为日志、社区、消息，以及业务侧需要稳定主键的影子论文记录

具体调用链路可以分成两段理解。

### 第一段：业务后端如何调用图数据库

后端通过 `GraphDatabaseConfig.java` 创建 Neo4j Java Driver，但它不是无条件启用的，而是受 `graph.neo4j.enabled=true` 控制。也就是说，只有在运行环境中显式开启 Neo4j 配置后，后端才会创建图数据库连接。

在此基础上，`GraphPaperService.java` 负责封装对 Neo4j 的论文查询，核心能力包括：

1. 按 `aminer_id` 查询单篇论文  
2. 按 `aminer_id` 批量查询多篇论文  
3. 基于标题、摘要、关键词执行搜索  
4. 按被引量与年份列出热门论文  
5. 批量 upsert `Paper` 节点

真正把图数据库接到业务链路里的关键在 `PaperServiceImpl.java`。它的逻辑不是“查不到就报错”，而是：

1. **如果 Neo4j 已启用**，论文列表、搜索、按 AMiner ID 查询会优先走图数据库  
2. 查询到 `Paper` 节点后，会立即执行 `upsertShadowPaper(...)`  
3. 该方法会把图中的论文信息同步成一条 **MySQL 影子论文记录**  
4. 后续业务模块依然可以继续使用本地 `paper.id` 完成详情页、行为日志、收藏、推荐结果跳转等操作

因此，图数据库在这里承担的是**论文主数据主库**角色，而 MySQL 中的 `paper` 表承担的是**业务兼容层**角色。

### 第二段：推荐链路如何借助图数据库完成闭环

推荐调用链路仍然是：

> 前端 `/api/recommend` → `RecommendController` → `RecommendServiceImpl` → `PythonRecClient` → Python `/recommend`

但和原来不同的是，当前链路中使用了**AMiner ID 作为跨服务统一标识**。

在 `RecommendServiceImpl.java` 中：

1. 先从 `behavior_log` 中读取用户最近交互过的 `paper.id`
2. 再通过 `paperService.getPaperById(...)` 把这些本地 ID 转换为 `aminer_id`
3. 将 AMiner ID 历史列表传给 Python 推荐服务
4. Python 返回的推荐结果中，`paper_id` 字段本质上也是 **AMiner ID**
5. 后端随后调用 `paperService.getByAminers(...)` 进行批量补全

这里的关键变化在于 `paperService.getByAminers(...)`：  
它会先查 MySQL 已有影子论文；对于缺失的论文，再去 Neo4j 查 `Paper` 节点；查到之后立即补写到 MySQL，并返回补全后的 `Paper` 对象。

这样就解决了原先“推荐结果能返回，但论文没插入 MySQL，导致详情页打不开、搜索不到、行为日志无法落库”的问题。  
换句话说，**推荐服务负责返回统一学术标识，后端负责把图数据库主数据映射为业务可消费的本地实体**，两者共同完成一致性闭环。

### 第三段：推荐服务本身如何调用图数据库

在 Python 侧，`RecommendationService._init_kg(...)` 决定知识图谱数据从哪里加载：

- 当 `config.graph_backend == "neo4j"` 时：
  1. `GraphStorage.load_from_neo4j(...)` 从 Neo4j 读取整张图
  2. `_extract_papers_from_kg(...)` 从图节点中提取 `Paper` 节点，恢复为论文目录
  3. 使用该论文目录构建 `CandidateGenerator.from_papers(...)`
  4. 同时基于整张图初始化 `KGEmbedder`

- 当 `graph_backend` 不是 `neo4j` 时：
  1. `AMinerLoader` 从本地数据集加载论文、作者、引用
  2. `KGBuilder` 在内存中重建知识图谱
  3. 再初始化候选生成器和知识图谱嵌入器

因此，在当前实现中，**Neo4j 不只是存档用数据库，而是推荐服务初始化候选集与图结构特征的直接数据源**。

---

## 25. 推荐服务现在的数据导入过程，以及三个脚本文件分别做什么？

**回答：**

当前推荐服务的数据导入过程分为**离线迁移阶段**和**在线运行阶段**两层。

### 一、离线迁移阶段

离线迁移的目标是：把原先分散在 MySQL、AMiner 数据集、旧知识图谱表中的数据，统一汇总进 Neo4j。

这一步由三个文件协同完成。

#### 1. `rl-service/scripts/neo4j_schema.cypher`

这个文件不是导入数据本身，而是负责**建立图数据库约束和索引**，属于导入前的结构准备脚本。  
它主要做三件事：

1. 为 `Paper`、`Author`、`Keyword`、`Venue`、`GraphNode` 创建唯一约束  
2. 为论文标题、年份、被引量等高频查询字段建立索引  
3. 保证后续 `MERGE` 导入时不会产生重复节点

可以把它理解为 Neo4j 版本的“建表与建索引脚本”。

#### 2. `rl-service/scripts/migrate_to_neo4j.py`

这是**主迁移脚本**，负责把多源数据整理后导入 Neo4j。  
它的数据来源主要有三类：

1. **MySQL `paper` 表**
   - 读取已有论文的标题、摘要、作者、关键词、会议、年份、被引量、embedding、MySQL 主键等信息
2. **AMiner 原始数据**
   - 补齐 MySQL 中缺失的摘要、关键词、作者、年份、引用信息
3. **旧版 `kg_entity` / `kg_relation` 表**
   - 作为历史知识图谱资产导入为 `LegacyEntity` 与 `LEGACY_RELATION`

该脚本导入后的主要图结构包括：

- `Paper` 节点
- `Author` 节点
- `Keyword` 节点
- `Venue` 节点
- `AUTHOR_OF` 关系
- `HAS_KEYWORD` 关系
- `PUBLISH_IN` 关系
- `CITE` 关系
- `CO_AUTHOR` 关系

也就是说，这个脚本完成的是**从关系型/文件型数据到异构学术图的全量建图**。

#### 3. `rl-service/scripts/backfill_mysql_shadow_papers.py`

这个脚本的作用和主迁移脚本不同，它不是“把数据导入 Neo4j”，而是**把 Neo4j 中已经存在的 `Paper` 节点反向回填到 MySQL 的 `paper` 表**。

它主要用于两个场景：

1. 首次切换到“图数据库为论文主库”时，为业务系统批量生成影子论文
2. 图数据库已更新，但 MySQL 业务影子层尚未补齐时，执行一次一致性回填

它会读取 Neo4j 中的 `Paper` 节点字段，然后用 `INSERT ... ON DUPLICATE KEY UPDATE` 的方式写入 MySQL，因此既能补新数据，也能同步更新旧数据。

### 二、在线运行阶段

离线迁移完成之后，推荐服务在线运行时的数据进入方式如下：

1. **推荐服务启动时**
   - `RecommendationService` 根据 `graph_backend` 决定是从 Neo4j 还是本地 AMiner 数据集加载图

2. **如果配置为 Neo4j**
   - 启动阶段直接从 Neo4j 加载整张知识图谱
   - 从图中抽取 `Paper` 节点构建候选论文池
   - 基于整张图构建知识图谱嵌入器

3. **用户发起推荐时**
   - 后端传入 AMiner ID 历史列表
   - Python 推荐服务基于当前图结构、候选集和用户特征生成 Top-K 推荐
   - 返回结果后，后端再按需把缺失论文补写为 MySQL 影子记录

因此，当前数据流可以概括为：

> **离线脚本负责“把数据建进图里”，在线服务负责“从图里读数据做推荐”，业务后端负责“把图中的论文映射为可落业务的 MySQL 影子实体”。**

---

## 26. 如何在运行环境里正确配置 Neo4j？

**回答：**

这一部分建议按“Neo4j 服务本身 → 图结构初始化 → 数据迁移 → 后端配置 → 推荐服务配置 → 联调验证”的顺序执行。

### 第一步：准备 Neo4j 服务

无论使用本机安装、Docker，还是服务器部署，都需要先得到以下四个核心参数：

1. **Bolt 地址**，例如 `bolt://127.0.0.1:7687`
2. **用户名**，默认通常为 `neo4j`
3. **密码**
4. **数据库名**，默认通常为 `neo4j`

如果是首次安装 Neo4j，通常还需要先完成一次初始化密码设置。

### 第二步：先执行图结构初始化脚本

在把业务数据导入图数据库之前，先执行：

`rl-service/scripts/neo4j_schema.cypher`

这样可以提前创建唯一约束和索引，避免迁移时产生重复节点，并提升后续查询性能。

如果使用 `cypher-shell`，典型执行方式是：

```powershell
cypher-shell -a neo4j://127.0.0.1:7687 -u neo4j -p <你的密码> -d neo4j -f rl-service\scripts\neo4j_schema.cypher
```

### 第三步：执行数据迁移脚本

主迁移脚本为：

`rl-service/scripts/migrate_to_neo4j.py`

它支持通过命令行参数或环境变量传入 MySQL 与 Neo4j 配置。  
在 Windows PowerShell 下，可采用下面这种方式：

```powershell
$env:GRAPH_NEO4J_URI="bolt://127.0.0.1:7687"
$env:GRAPH_NEO4J_USERNAME="neo4j"
$env:GRAPH_NEO4J_PASSWORD="<你的密码>"
$env:GRAPH_NEO4J_DATABASE="neo4j"
$env:MYSQL_HOST="127.0.0.1"
$env:MYSQL_PORT="3306"
$env:MYSQL_DB="research_db"
$env:MYSQL_USER="root"
$env:MYSQL_PASSWORD="<你的MySQL密码>"

python rl-service\scripts\migrate_to_neo4j.py --paper-limit 1000 --batch-size 500
```

如果希望清空旧图后重建，可增加 `--clear`。  
如果只想跳过某一数据源，也可以使用 `--skip-mysql`、`--skip-aminer`、`--skip-legacy-kg`。

### 第四步：回填 MySQL 影子论文表

迁移完成后，建议立即执行一次：

```powershell
python rl-service\scripts\backfill_mysql_shadow_papers.py
```

这样可以保证 MySQL 的 `paper` 表中已经存在和 Neo4j 一致的影子论文，避免业务系统首次访问推荐结果时临时补写过多数据。

### 第五步：配置 Spring Boot 后端

后端的 Neo4j 配置写在 `backend/src/main/resources/application.yml` 中，但实际采用的是**环境变量占位**方式：

```yaml
graph:
  neo4j:
    enabled: ${GRAPH_NEO4J_ENABLED:false}
    uri: ${GRAPH_NEO4J_URI:bolt://localhost:7687}
    username: ${GRAPH_NEO4J_USERNAME:neo4j}
    password: ${GRAPH_NEO4J_PASSWORD:}
    database: ${GRAPH_NEO4J_DATABASE:neo4j}
```

因此，运行后端前，至少要配置下面五个变量：

```powershell
$env:GRAPH_NEO4J_ENABLED="true"
$env:GRAPH_NEO4J_URI="bolt://127.0.0.1:7687"
$env:GRAPH_NEO4J_USERNAME="neo4j"
$env:GRAPH_NEO4J_PASSWORD="<你的密码>"
$env:GRAPH_NEO4J_DATABASE="neo4j"
```

然后再启动后端：

```powershell
cd backend
mvn spring-boot:run
```

这里最重要的是 `GRAPH_NEO4J_ENABLED=true`。  
如果这个值没有打开，`GraphDatabaseConfig` 就不会创建 Neo4j Driver，后端也不会真正走图数据库逻辑。

### 第六步：配置 Python 推荐服务

Python 推荐服务现在也支持通过环境变量读取 Neo4j 配置。  
默认情况下，`rl-service/config.py` 会优先读取以下环境变量：

```powershell
$env:REC_GRAPH_BACKEND="neo4j"
$env:GRAPH_NEO4J_URI="bolt://127.0.0.1:7687"
$env:GRAPH_NEO4J_USERNAME="neo4j"
$env:GRAPH_NEO4J_PASSWORD="<你的密码>"
$env:GRAPH_NEO4J_DATABASE="neo4j"
```

其中最关键的是：

1. `REC_GRAPH_BACKEND` 必须设为 `"neo4j"`
2. `GRAPH_NEO4J_PASSWORD` 必须填写正确
3. `GRAPH_NEO4J_DATABASE` 要和实际数据库名一致

随后启动 Python 服务：

```powershell
cd rl-service
uvicorn api.server:app --host 0.0.0.0 --port 8000
```

### 第七步：做联调验证

建议至少验证以下四项：

1. **Neo4j 是否有数据**
   - 例如执行 `MATCH (p:Paper) RETURN count(p);`

2. **后端是否启用了 Neo4j**
   - 访问论文搜索或推荐详情补全接口，确认可读取仅存在于图中的论文

3. **Python 推荐服务是否从图加载成功**
   - 启动日志中应出现类似“KG 从 Neo4j 加载完成”的提示

4. **推荐结果是否能回到业务主键体系**
   - 调用 `/api/recommend` 后，返回结果中的论文应能正常进入详情页、被搜索到、被记录行为日志

### 第八步：运行环境中的配置建议

从工程实践上，建议把 Neo4j 配置拆成两层管理：

1. **后端运行环境变量**
   - 管 Spring Boot 是否启用图数据库、连接哪个 Neo4j 实例

2. **Python 推荐环境变量**
   - 管推荐服务启动时从哪里加载知识图谱

当前代码已经支持这种分工。  
因此在实际部署中，最稳妥的做法是：

- 后端使用环境变量
- Python 服务也使用同一组环境变量
- 迁移脚本与回填脚本使用同一组连接参数

这样可以确保**迁移脚本、业务后端、推荐服务三者连接的是同一个 Neo4j 库**，避免出现“脚本写到了 A 库、推荐服务读的是 B 库、后端连的是 C 库”的配置错位问题。
